---
phase: 02-auth-crypto
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - package.json
  - pnpm-lock.yaml
  - apps/api/src/main.ts
  - apps/api/src/app.module.ts
autonomous: true
must_haves:
  truths:
    - "apps/api builds + boots on @nestjs/* ^11 (core, common, platform-express, config, schematics, cli)"
    - "Helmet, ValidationPipe, AllExceptionsFilter, nestjs-pino, /health all still work after upgrade"
    - "pnpm.overrides for `multer@<2.1.1: >=2.1.1` REMOVED from root package.json (Nest 11 ships compatible multer)"
    - "pnpm.overrides for `lodash@<4.18.0: >=4.18.1` REMAINS (separate upstream issue, unrelated to Nest)"
    - "`pnpm audit --prod --audit-level=high` returns 0 high/critical advisories"
    - "GET /health still returns canonical HealthResponse shape (degraded if DB/Redis unreachable)"
  artifacts:
    - "apps/api/package.json with @nestjs/* on ^11"
    - "Root package.json with multer override removed"
    - "Refreshed pnpm-lock.yaml"
  key_links:
    - "@nestjs/common, @nestjs/core, @nestjs/platform-express, @nestjs/config, @nestjs/cli, @nestjs/schematics MUST be upgraded together — mixing 10/11 across these breaks Reflect metadata"
    - "nestjs-pino must be on a 4.x line that declares Nest 11 peer (verify on npmjs before bumping)"
    - "After upgrade, re-run `pnpm audit` and confirm both the dev-only @nestjs/cli@10.4.9 Highs (glob CLI / picomatch ReDoS) are GONE"
---

<objective>
Upgrade NestJS 10.4.x → 11.x in `apps/api` as the FIRST plan of Phase 02. Ship no new feature work in this commit — pure tooling bump. Removes the `pnpm.overrides` for `multer@<2.1.1` (Nest 11 ships compatible multer) and clears the dev-only `@nestjs/cli@10.4.9` glob/picomatch Highs flagged in Phase 01.

Why first: every subsequent Phase 02 API plan (07, 08, 09) lands on NestJS. Doing the framework bump up front avoids re-doing all of those plans against a moving target.
</objective>

<execution_context>
Atomic commits per task. Suggested commit messages:
- `chore(02-01-T1): upgrade @nestjs/* to ^11.x`
- `chore(02-01-T2): remove multer pnpm.overrides (Nest 11 ships compatible)`
- `chore(02-01-T3): refresh pnpm audit baseline post-upgrade`
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-auth-crypto/02-INDEX.md
@apps/api/package.json
@package.json
@apps/api/src/main.ts
@apps/api/src/app.module.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump @nestjs/* and peer deps to v11</name>
  <files>
    - apps/api/package.json
    - pnpm-lock.yaml
  </files>
  <action>
Update `apps/api/package.json` deps to:
- `@nestjs/common`: `^11.0.0`
- `@nestjs/core`: `^11.0.0`
- `@nestjs/platform-express`: `^11.0.0`
- `@nestjs/config`: latest stable line that declares Nest 11 peer (likely `^4.0.0` — verify on npm)
- `@nestjs/cli` (dev): `^11.0.0`
- `@nestjs/schematics` (dev): `^11.0.0`
- `nestjs-pino`: latest stable that supports Nest 11 (likely `^4.x` — verify peer range)
- `pino`, `pino-http`, `helmet`, `ioredis`, `reflect-metadata`, `rxjs`, `zod`: keep current; bump only if Nest 11 peer changed (`rxjs ^7.8` is fine; verify)
- `class-validator` + `class-transformer`: leave as-is (already in apps/api/package.json from Phase 01 deviations)

Run `pnpm install` from repo root. Confirm the lockfile updates and no peer-dep errors are raised. If peer-dep complaints surface from `nestjs-pino` against Nest 11, EITHER bump nestjs-pino to its v5/v6 line OR pin Nest 11.0.0 (latest, not range floor) — document the choice in the SUMMARY.

Build + typecheck:
- `pnpm --filter @simplevault/api typecheck`
- `pnpm --filter @simplevault/api build`

Both must exit 0. Nest 11 has a couple of API renames (e.g. `nestjs/microservices` minor changes) but apps/api uses only common/core/platform-express which are source-compatible.

Do NOT change main.ts in this task — only verify it still compiles.
  </action>
  <verify>
- `apps/api/package.json` lists `@nestjs/common`/`@nestjs/core`/`@nestjs/platform-express` on `^11.x`.
- `pnpm-lock.yaml` resolves a single `@nestjs/common@11.x` (no duplicate v10).
- `pnpm --filter @simplevault/api build` exits 0.
- `pnpm --filter @simplevault/api typecheck` exits 0.
- `node apps/api/dist/main.js` boots on :3001 (with no DB env, expect degraded — same Phase 01 behaviour).
- `curl http://localhost:3001/health` returns 200 with `degraded`.
  </verify>
  <done>
apps/api on NestJS 11; build + boot + /health unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove multer pnpm.overrides + verify pnpm audit clean</name>
  <files>
    - package.json (root)
    - pnpm-lock.yaml
  </files>
  <action>
Open root `package.json`. In the `"pnpm": { "overrides": { … } }` block, REMOVE the `"multer@<2.1.1"` entry. Leave the `"lodash@<4.18.0"` entry in place (different upstream advisory, unrelated to Nest).

Run `pnpm install` from root. Lockfile should update; verify `pnpm-lock.yaml` shows `multer` resolving to a `>=2.x` version naturally (because Nest 11's `@nestjs/platform-express` declares a fresh enough peer).

Run `pnpm audit --prod --audit-level=high`. Expect: 0 high, 0 critical.

Run `pnpm audit --audit-level=high` (includes dev). The two dev-only Highs from Phase 01 (`glob` CLI command-injection + `picomatch` ReDoS via `@nestjs/cli@10.4.9`) MUST be gone. If `pnpm audit --audit-level=high` still reports anything, capture the output in the SUMMARY and stop — operator decides per-finding (fix vs. accept-and-document).
  </action>
  <verify>
- `grep -n multer package.json` shows ONLY non-override mentions (or zero mentions).
- `pnpm audit --prod --audit-level=high` reports 0 advisories.
- `pnpm audit --audit-level=high` reports 0 advisories (or only acceptable residuals documented in SUMMARY).
- `apps/api` still builds + boots.
  </verify>
  <done>
multer override gone; audit baseline clean; lockfile committed.
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @simplevault/api build` green.
- `pnpm --filter @simplevault/api typecheck` green.
- Root `pnpm audit --audit-level=high` reports 0 high/critical (or only documented residuals).
- `docker compose up -d api` (after rebuild of the api image) still healthchecks green; `curl http://localhost:3001/health` returns 200.
- `pnpm-lock.yaml` resolves @nestjs/common@11.x as the sole NestJS major.
</verification>

<success_criteria>
- Two atomic commits.
- No code changes outside `apps/api/package.json`, root `package.json`, and `pnpm-lock.yaml` (Task 3 if needed for any tiny main.ts shim adjustment).
- Phase-01 multer override removed; lodash override retained.
- All 6 truths above observably TRUE.
</success_criteria>

<output>
Create `.planning/phases/02-auth-crypto/02-01-SUMMARY.md` documenting: NestJS minor.patch landed, peer-dep adjustments to nestjs-pino if any, exact `pnpm audit` output (before/after), any breaking-change source edits required, deviations from spec.
</output>
