---
phase: 01-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - turbo.json
  - tsconfig.base.json
  - .gitignore
  - .npmrc
  - .editorconfig
  - .nvmrc
  - apps/.gitkeep
  - packages/.gitkeep
autonomous: true
must_haves:
  truths:
    - "pnpm install from clean clone succeeds with no errors"
    - "pnpm-workspace.yaml resolves all apps/* and packages/* globs"
    - "turbo.json defines build, lint, typecheck, dev tasks"
    - ".nvmrc pins Node 22 LTS; .npmrc enforces strict-peer-dependencies and no auto-install of optional deps from postinstall"
  artifacts:
    - "package.json (root, private:true, packageManager:pnpm@9+)"
    - "pnpm-workspace.yaml (apps/*, packages/*)"
    - "turbo.json (tasks: build, lint, typecheck, dev with proper dependsOn)"
    - "tsconfig.base.json (strict, target ES2023, moduleResolution bundler)"
    - ".gitignore (node_modules, .next, dist, .turbo, .env*, coverage)"
    - ".npmrc (strict-peer-dependencies=true, auto-install-peers=false, ignore-scripts already-not-set so we audit per-dep)"
    - ".editorconfig (LF, utf-8, 2-space indent, final-newline)"
    - ".nvmrc (22)"
  key_links:
    - "turbo.json task dependsOn correctness (^build means each pkg's deps must build first)"
    - ".npmrc strict mode catches peer-dep mismatches that would otherwise silently break runtime"
---

<objective>
Bootstrap the Turborepo + pnpm monorepo. Empty workspaces, zero apps/packages content yet — just the scaffolding that everything else will mount onto.

Purpose: every subsequent plan depends on this. If Turbo task graph or pnpm workspace globbing is wrong, parallel execution downstream silently breaks.
Output: a clonable repo where `pnpm install` works and `pnpm exec turbo run build` returns "no tasks" cleanly.
</objective>

<execution_context>
You are the executor. Follow the GSD execution workflow. Atomic commits per task with message format `feat(01-01-T<N>): <description>`.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Turborepo + pnpm workspace at root</name>
  <files>
    - package.json
    - pnpm-workspace.yaml
    - turbo.json
    - .npmrc
    - .nvmrc
    - .gitignore
    - .editorconfig
  </files>
  <action>
Create the following files:

**package.json** (root):
- `"private": true`
- `"name": "simplevault"`
- `"packageManager": "pnpm@9.15.0"` (or latest stable 9.x)
- `"engines": { "node": ">=22.0.0", "pnpm": ">=9.0.0" }`
- Scripts:
  - `"dev": "turbo run dev"`
  - `"build": "turbo run build"`
  - `"lint": "turbo run lint"`
  - `"typecheck": "turbo run typecheck"`
  - `"test": "turbo run test"`
  - `"clean": "turbo run clean && rm -rf node_modules"`
- devDependencies (workspace tooling only — no app deps): `"turbo": "^2.3.0"`, `"typescript": "~5.7.0"`, `"@types/node": "^22.0.0"`, `"prettier": "^3.4.0"`

**pnpm-workspace.yaml**:
```yaml
packages:
  - "apps/*"
  - "packages/*"
```

**turbo.json**:
- `"$schema": "https://turbo.build/schema.json"`
- Define tasks:
  - `build`: `dependsOn: ["^build"]`, `outputs: [".next/**", "!.next/cache/**", "dist/**"]`
  - `lint`: `dependsOn: ["^build"]`, `cache: true`
  - `typecheck`: `dependsOn: ["^build"]`, `cache: true`
  - `dev`: `cache: false`, `persistent: true`
  - `test`: `dependsOn: ["^build"]`, `outputs: ["coverage/**"]`
  - `clean`: `cache: false`
- Global `passThroughEnv: ["DATABASE_URL", "REDIS_URL", "JWT_SECRET", "SERVER_CHAIN_SECRET", "NODE_ENV"]`

**.npmrc**:
```
strict-peer-dependencies=true
auto-install-peers=false
shared-workspace-lockfile=true
shamefully-hoist=false
node-linker=isolated
```
(node-linker=isolated keeps strict — catches deps that import things they don't declare.)

**.nvmrc**: just `22`

**.gitignore**: standard Node + Turborepo. Include: `node_modules/`, `.next/`, `dist/`, `.turbo/`, `coverage/`, `.env`, `.env.local`, `.env.*.local`, `*.log`, `.DS_Store`, `tmp/`, `out/`. Do NOT ignore `.env.example`.

**.editorconfig**: root=true, charset=utf-8, end_of_line=lf, indent_style=space, indent_size=2, insert_final_newline=true, trim_trailing_whitespace=true. Override for Markdown: `trim_trailing_whitespace = false`.

After writing all files, run `pnpm install` from root. Should succeed and create `pnpm-lock.yaml`.
  </action>
  <verify>
- `cat package.json | jq .name` returns `"simplevault"`.
- `pnpm install` exits 0 with no peer-dep errors.
- `pnpm-lock.yaml` exists and is committed.
- `pnpm exec turbo run build` exits 0 with "No tasks were executed as part of this run." (no apps yet, so nothing to build).
  </verify>
  <done>
Root config files exist; `pnpm install` succeeds; lockfile committed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create empty workspace structure + base TS config</name>
  <files>
    - tsconfig.base.json
    - apps/.gitkeep
    - packages/.gitkeep
  </files>
  <action>
**tsconfig.base.json** (extended by every package):
```json
{
  "compilerOptions": {
    "target": "ES2023",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2023"],
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "noFallthroughCasesInSwitch": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "exactOptionalPropertyTypes": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "verbatimModuleSyntax": true,
    "incremental": true,
    "tsBuildInfoFile": "./.tsbuildinfo"
  },
  "exclude": ["node_modules", "dist", ".next", ".turbo"]
}
```

Create `apps/.gitkeep` and `packages/.gitkeep` (empty files) so the dirs are tracked.

Run `pnpm install` again to confirm everything still resolves. Run `git status` to verify all the right files are tracked.
  </action>
  <verify>
- `find apps packages -type d -maxdepth 1` lists both dirs.
- `pnpm install` exits 0.
- `git ls-files | grep -E '(\.gitkeep|tsconfig\.base\.json|\.editorconfig|\.npmrc|\.nvmrc)$'` shows all five.
  </verify>
  <done>
Workspace dirs exist (with `.gitkeep`); base TS config in place; ready for downstream packages to extend it.
  </done>
</task>

</tasks>

<verification>
- `pnpm install` from clean (delete `node_modules` first) succeeds in <60s.
- `pnpm exec turbo run build` shows "No tasks executed".
- `git status` shows no untracked files (everything committed).
- Lockfile (`pnpm-lock.yaml`) exists.
</verification>

<success_criteria>
- All 8 root config files exist with the contents specified.
- `apps/` and `packages/` directories exist with `.gitkeep`.
- `pnpm install` succeeds; lockfile committed.
- Both tasks committed with messages `feat(01-01-T1): initialize turborepo + pnpm workspace` and `feat(01-01-T2): add base tsconfig + workspace dirs`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations/01-01-SUMMARY.md` documenting: files created, lockfile size (sanity check), any deviations from spec.
</output>
