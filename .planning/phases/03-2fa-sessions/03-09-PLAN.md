---
phase: 03-2fa-sessions
plan: 09
type: execute
wave: 5
depends_on: [03-02, 03-03, 03-05, 03-06, 03-07, 03-08]
files_modified:
  - apps/api/src/auth/jwt/jwt-auth.guard.ts (add @Public() recognition)
  - apps/api/src/auth/jwt/public.decorator.ts (new — `@Public()`)
  - apps/api/src/app.module.ts (move JwtAuthGuard from per-controller to APP_GUARD; ordering before throttler)
  - apps/api/src/common/throttler.config.ts (login-email keying via sha256 + new ceilings consolidated)
  - apps/api/src/auth/login/login.controller.ts (drop @UseGuards(JwtAuthGuard) — never had it but verify)
  - apps/api/src/auth/refresh/refresh.controller.ts (add @Public)
  - apps/api/src/auth/logout/logout.controller.ts (add @Public)
  - apps/api/src/invite/invite.controller.ts (add @Public on /invite/redeem)
  - apps/api/src/health/health.controller.ts (add @Public)
  - apps/api/src/auth/login/login.controller.ts (add @Public on /auth/params + /auth/login)
  - apps/api/src/twofa/webauthn/webauthn-auth.controller.ts (begin-auth + finish-auth: NO @Public — uses Require2FAStepUpGuard which validates step-up token instead)
  - apps/api/src/twofa/totp/totp.controller.ts (verify: same — uses Require2FAStepUpGuard)
autonomous: true
must_haves:
  truths:
    - "JwtAuthGuard registered as APP_GUARD; runs BEFORE SimpleVaultThrottlerGuard for any non-@Public route. `req.user.id` is populated when the throttler's generateKey runs"
    - "@Public() decorator allow-list: /health, /auth/login, /auth/refresh, /auth/logout, /auth/params, /invite/redeem (preserve), /2fa/webauthn/begin-auth, /2fa/webauthn/finish-auth, /2fa/totp/verify (these use Require2FAStepUpGuard instead — opt out of JwtAuthGuard)"
    - "Throttler `me-user` ceiling now correctly keys by req.user.id (FINDING-0021 closed). Verified by Phase 02 reproduction test: same JWT from two different IPs both hit the same 100/min/user budget"
    - "Throttler `login-email` keying via `tracker = 'em:' + sha256(body.email.toLowerCase()).slice(0,16)` — fixed-length, no PII in Redis, no key-flooding amplification (FINDING-0022 closed)"
    - "All Phase-03 user-keyed ceilings (2fa-register-user, 2fa-list-user, 2fa-verify-user, sessions-list-user, sessions-revoke-user, sessions-revoke-all-user) correctly key by req.user.id"
    - "Existing 21/21 Phase-02 E2E pass + new endpoints continue to work (regression-free reorder)"
  artifacts:
    - "@Public() decorator + Reflector check in JwtAuthGuard"
    - "APP_GUARD entry in AppModule for JwtAuthGuard before throttler"
    - "Updated throttler login-email keying"
  key_links:
    - "Order matters: APP_GUARDs in NestJS run in registration order. Register JwtAuthGuard FIRST, then SimpleVaultThrottlerGuard. Verified by `grep -n 'APP_GUARD' apps/api/src/app.module.ts` showing JwtAuthGuard's provider above ThrottlerGuard's"
    - "@Public decorator: a metadata-key decorator. JwtAuthGuard's canActivate calls `this.reflector.getAllAndOverride<boolean>(IS_PUBLIC_KEY, [ctx.getHandler(), ctx.getClass()])` — if true, skip auth check"
    - "Step-up routes (/2fa/webauthn/{begin,finish}-auth + /2fa/totp/verify) use Require2FAStepUpGuard which validates the step-up token. They MUST NOT have JwtAuthGuard run on them (token has purpose:'2fa-stepup' which JwtAuthGuard rejects). Solution: mark these routes @Public for JwtAuthGuard, then apply Require2FAStepUpGuard via @UseGuards"
    - "FINDING-0022 fix: hash the email before keying. Why slice(0,16)? 16 hex chars = 64 bits = collision rate negligible at our scale; bounds key length so an attacker can't flood Redis with 10KB keys. PII-free because sha256 is one-way"
---

<objective>
Close the throttler ordering bug (FINDING-0021) AND the login-email key-flooding (FINDING-0022) in a single coordinated change. This Plan unblocks all of Phase-03's new user-keyed throttler ceilings — without it, every new ceiling silently falls back to IP-keying.

ALSO sets up the `@Public()` opt-out pattern that Phase 04+ controllers will use.

Three atomic commits.
</objective>

<execution_context>
Atomic commits per task:
- `refactor(03-09-T1): @Public() decorator + JwtAuthGuard reads it via Reflector`
- `refactor(03-09-T2): JwtAuthGuard moved to APP_GUARD before throttler — FINDING-0021 closure`
- `fix(03-09-T3): throttler login-email keying via sha256(email).slice(0,16) — FINDING-0022 closure`
</execution_context>

<context>
@.planning/security/FINDINGS.md
@.planning/phases/03-2fa-sessions/03-INDEX.md
@.planning/phases/03-2fa-sessions/03-02-PLAN.md
@.planning/phases/03-2fa-sessions/03-04-PLAN.md
@.planning/phases/03-2fa-sessions/03-05-PLAN.md
@.planning/phases/03-2fa-sessions/03-06-PLAN.md
@apps/api/src/auth/jwt/jwt-auth.guard.ts
@apps/api/src/common/throttler.config.ts
@apps/api/src/app.module.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: @Public() decorator + JwtAuthGuard Reflector integration</name>
  <files>
    - apps/api/src/auth/jwt/public.decorator.ts (new)
    - apps/api/src/auth/jwt/jwt-auth.guard.ts (extend)
  </files>
  <action>
```ts
// public.decorator.ts
import { SetMetadata } from "@nestjs/common";
export const IS_PUBLIC_KEY = "isPublic";
export const Public = () => SetMetadata(IS_PUBLIC_KEY, true);
```

In `JwtAuthGuard`:
```ts
constructor(private readonly jwt: JwtService, private readonly epochCache: SessionEpochCache, private readonly reflector: Reflector) {}

async canActivate(ctx: ExecutionContext): Promise<boolean> {
  const isPublic = this.reflector.getAllAndOverride<boolean>(IS_PUBLIC_KEY, [ctx.getHandler(), ctx.getClass()]);
  if (isPublic) return true;
  // ... existing canActivate body ...
}
```

This change alone is harmless — JwtAuthGuard is still per-controller until T2. But T1 lays the foundation.
  </action>
  <verify>
- Build + typecheck green. Existing 21/21 E2E pass.
- Unit-spec or in-place: a controller method decorated `@Public()` that's behind `@UseGuards(JwtAuthGuard)` returns 200 even without an Authorization header.
  </verify>
  <done>
@Public() works; JwtAuthGuard reads it.
  </done>
</task>

<task type="auto">
  <name>Task 2: Move JwtAuthGuard to APP_GUARD; @Public-decorate every public route</name>
  <files>
    - apps/api/src/app.module.ts (APP_GUARD provider for JwtAuthGuard, before ThrottlerGuard)
    - apps/api/src/auth/login/login.controller.ts (@Public on POST /auth/login + GET /auth/params)
    - apps/api/src/auth/refresh/refresh.controller.ts (@Public)
    - apps/api/src/auth/logout/logout.controller.ts (@Public)
    - apps/api/src/auth/signup/signup.controller.ts (@Public)
    - apps/api/src/invite/invite.controller.ts (@Public on /invite/redeem)
    - apps/api/src/health/health.controller.ts (@Public)
    - apps/api/src/twofa/webauthn/webauthn-auth.controller.ts (@Public on begin-auth + finish-auth — they use Require2FAStepUpGuard for actual auth)
    - apps/api/src/twofa/totp/totp.controller.ts (@Public on /verify — same)
    - drop @UseGuards(JwtAuthGuard) from controllers where it becomes redundant (optional cleanup; keeping it is harmless)
  </files>
  <action>
**`app.module.ts`** providers list — IMPORTANT order:
```ts
providers: [
  // ... existing ...
  { provide: APP_GUARD, useClass: JwtAuthGuard },     // FIRST — runs before throttler
  { provide: APP_GUARD, useClass: SimpleVaultThrottlerGuard }, // SECOND
  // ... rest ...
],
```

Confirmed by NestJS docs: `APP_GUARD`s run in registration order; the second one only fires if the first allowed.

**Decorate every public route**:
- Run `grep -rnE "@Controller" apps/api/src` → enumerate controllers. For each controller method, decide: is it public (no auth required) or auth-required?
- Public list (mark with `@Public()`):
  - Health: `GET /health`.
  - Invite: `POST /invite/redeem` (anyone with a code).
  - Auth: `POST /auth/login`, `POST /auth/refresh`, `POST /auth/logout` (last 3 read cookie), `GET /auth/params` (public salt), `POST /auth/signup`.
  - 2FA step-up routes: `POST /2fa/webauthn/begin-auth`, `POST /2fa/webauthn/finish-auth`, `POST /2fa/totp/verify` — they're guarded by Require2FAStepUpGuard which provides actual auth via the step-up token; JwtAuthGuard would reject the step-up token.
- Auth-required (no `@Public()` — JwtAuthGuard runs):
  - `GET /me`.
  - `POST /2fa/webauthn/begin-register`, `/finish-register`.
  - `POST /2fa/totp/begin-register`, `/finish-register`.
  - `GET /2fa/methods`, `DELETE /2fa/methods/:id`.
  - `GET /sessions`, `DELETE /sessions/:id`, `POST /sessions/revoke-all`.
  - `POST /vault/_2fa-guard-probe` (test-only).

**Verify by grep**:
```
$ grep -rnE "@(Get|Post|Put|Delete|Patch)\(" apps/api/src
```
Cross-reference each method against the @Public list. Anything not on the list MUST run JwtAuthGuard now. Document the enumeration in 03-09-SUMMARY.

**Verify the throttler order**: deliberately reproduce FINDING-0021. Log in user A, take their JWT. From IP-1, send 101 `/me` requests (101st = 429). From IP-2, send `/me` with the SAME JWT → expect immediate 429 (because user-keyed budget is exhausted). Pre-fix this would have given a fresh budget → confirms the fix.
  </action>
  <verify>
- `pnpm --filter @simplevault/api build && lint && typecheck` green.
- Existing 21/21 E2E pass (Phase-02 invariants).
- New behaviour: FINDING-0021 reproduction now blocks (101st request 429 from any IP).
- All public routes still return without 401 when called unauthenticated.
- All auth-required routes return 401 without an access JWT.
- Step-up routes accept step-up tokens via Require2FAStepUpGuard; reject access tokens via the existing JwtAuthGuard purpose-claim rejection.
  </verify>
  <done>
JwtAuthGuard is APP_GUARD; every route correctly classified; FINDING-0021 closed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Throttler login-email keying via sha256 — FINDING-0022 closure</name>
  <files>
    - apps/api/src/common/throttler.config.ts (modify generateKey)
  </files>
  <action>
In `SimpleVaultThrottlerGuard.generateKey`:
```ts
} else if (name === "login-email") {
  const body = req.body;
  const email = typeof body?.email === "string" ? body.email.toLowerCase() : "no-email";
  // FINDING-0022 fix: hash + slice to bound Redis key size and remove PII
  const hashed = createHash("sha256").update(email).digest("hex").slice(0, 16);
  tracker = `em:${hashed}`;
}
```

Add an import: `import { createHash } from "node:crypto";`.

Add a comment block at the top of the file documenting "throttler reads body BEFORE Zod validation runs (guards before pipes); any future ceiling that keys off body fields MUST also bound input length OR hash to a fixed length before keying."

**Reproduce FINDING-0022 + verify fix**:
- Send 5 `POST /auth/login` from a single IP, each body containing `{"email":"<10KB random>","argon2SecretKeyHash":"<valid 64 chars>"}`.
- Pre-fix: Redis keys `login-email:em:<10KB-string>` × 5 → ~50 KB Redis memory.
- Post-fix: Redis keys `login-email:em:<16-char-hex>` × 5 → 80 bytes Redis memory.
- Verify via `redis-cli --scan --pattern 'login-email:em:*' | head` post-test.
  </action>
  <verify>
- Build + typecheck green.
- E2E reproduction confirms keys are now fixed-length 16 hex chars.
- Existing login rate-limit (10/email/h) STILL functions: send 11 logins for the same email → 11th = 429.
  </verify>
  <done>
FINDING-0022 closed. Redis key size bounded.
  </done>
</task>

</tasks>

<verification>
- FINDINGS.md update: 0021 + 0022 → VERIFIED-CLOSED with re-run notes.
- Truths 18 + 19 in INDEX.
- Phase-02 E2E unaffected (21/21).
- Every Phase-03 user-keyed ceiling now correctly keys.
- `pnpm --filter @simplevault/api build && lint && typecheck` green.
</verification>

<success_criteria>
- Three atomic commits.
- Two FINDINGS closed (0021 + 0022).
- The full @Public allow-list documented in 03-09-SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/03-2fa-sessions/03-09-SUMMARY.md`. Document: full @Public route enumeration; the APP_GUARD ordering rule; the FINDING-0021 + 0022 reproduction + closure; rationale for hex-slice(0,16) over fixed-length encoding; defence-in-depth note that FINDING-0017 storage cap (Plan 01) is the second layer.
</output>
