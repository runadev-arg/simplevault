---
phase: 03-2fa-sessions
plan: 07
type: tdd
wave: 3
depends_on: [03-01, 03-02, 03-03, 03-06]
files_modified:
  - apps/api/src/vault/_2fa-guard-probe.controller.ts (new — gated by EXPOSE_TEST_ROUTES)
  - apps/api/src/vault/vault-probe.module.ts (new — only registered conditionally)
  - apps/api/src/app.module.ts (conditional registration)
  - apps/api/test/2fa-required-guard.spec.ts (new)
  - apps/api/.env.example (EXPOSE_TEST_ROUTES doc)
autonomous: true
must_haves:
  truths:
    - "When EXPOSE_TEST_ROUTES=1, POST /vault/_2fa-guard-probe returns 200 {ok:true} for users with ≥1 active 2FA method"
    - "When EXPOSE_TEST_ROUTES=1, POST /vault/_2fa-guard-probe returns 403 AUTH_2FA_REQUIRED for users with 0 active 2FA methods"
    - "When EXPOSE_TEST_ROUTES is undefined or != '1', /vault/_2fa-guard-probe returns 404 (route not registered) — verified via api boot inspection"
    - "Production Dockerfile DOES NOT set EXPOSE_TEST_ROUTES — verified by grep"
    - "The Require2FAGuard from Plan 06 is reused unchanged (single source of truth for the 2FA-required check)"
  artifacts:
    - "apps/api/src/vault/_2fa-guard-probe.controller.ts"
    - "Conditional registration in AppModule"
    - "Integration test 2fa-required-guard.spec.ts"
  key_links:
    - "Conditional registration: in app.module.ts use `imports: [...(process.env.EXPOSE_TEST_ROUTES === '1' ? [VaultProbeModule] : [])]`. Module is unimported in production builds, route is absent — verifiable via `curl /vault/_2fa-guard-probe` returning 404"
    - "Phase 07 will delete this probe + module and replace with the real `vault.create` / `vault.join` controllers using `@UseGuards(JwtAuthGuard, Require2FAGuard)`. Probe stays during Phase 03–07 transition for regression coverage; Phase 07 deletes it as the FIRST commit (hand-off rule documented in 03-INDEX Operator decisions §6)"
    - "TDD because the gate logic is small + deterministic (count of methods → 200 vs 403). RED is the integration test asserting both branches; GREEN is wiring up the conditional module + the controller"
---

<objective>
Ship the 2FA-required guard for the future `vault.*` routes, and prove it works via a probe endpoint that exists only in test/dev builds (`EXPOSE_TEST_ROUTES=1`). Production builds DO NOT register the probe route.

This is the LAST piece of Wave 3. After this lands, every API-side 2FA story is in place and Wave 4 wires `/auth/login` to branch on 2FA presence.

Two atomic commits.
</objective>

<execution_context>
Atomic commits per task:
- `test(03-07-T1): 2FA-required guard integration tests — RED`
- `feat(03-07-T2): conditional probe route + Require2FAGuard wiring — GREEN`
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/03-2fa-sessions/03-INDEX.md
@.planning/phases/03-2fa-sessions/03-06-PLAN.md
@apps/api/src/twofa/two-fa-guard.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1 (RED): integration tests for the probe route + guard branches</name>
  <files>
    - apps/api/test/2fa-required-guard.spec.ts
  </files>
  <action>
Write a Vitest integration test (mirror Plan 06's `2fa-removal.spec.ts` pattern) that boots a Nest test app with `EXPOSE_TEST_ROUTES=1` set in `process.env`:

1. Create user A (no 2FA enrolled). Hit `POST /vault/_2fa-guard-probe` with user A's JWT → expect 403 with body `{error:{code:"AUTH_2FA_REQUIRED", ...}}`.
2. Enrol a TOTP for user A. Re-hit → expect 200 `{ok:true}`.
3. Remove the TOTP (DELETE /2fa/methods/:id). Re-hit → expect 403 `AUTH_2FA_REQUIRED`.
4. Enrol a webauthn. Re-hit → 200.
5. Boot a SECOND Nest test app WITHOUT `EXPOSE_TEST_ROUTES`. Hit the same path → expect 404 (Nest router has no match; falls through to NotFoundException). This verifies the conditional registration.

Run `pnpm --filter @simplevault/api test -- 2fa-required-guard` → all RED (route doesn't exist yet).
  </action>
  <verify>
- The spec file exists and runs; every test fails (or skipped with TODO comment for T2).
  </verify>
  <done>
RED. Spec is the contract; T2 wires up.
  </done>
</task>

<task type="auto">
  <name>Task 2 (GREEN): conditional probe controller + module + AppModule wiring</name>
  <files>
    - apps/api/src/vault/_2fa-guard-probe.controller.ts
    - apps/api/src/vault/vault-probe.module.ts
    - apps/api/src/app.module.ts (conditional import)
    - apps/api/.env.example
  </files>
  <action>
**Controller**:
```ts
@Controller("vault")
export class TwoFaGuardProbeController {
  @Post("_2fa-guard-probe")
  @UseGuards(JwtAuthGuard, Require2FAGuard)
  probe(): { ok: true } { return { ok: true }; }
}
```

**Module**: `VaultProbeModule` imports `TwoFaModule` (for `Require2FAGuard` + `MethodsService`), declares `TwoFaGuardProbeController`. NO providers of its own.

**`app.module.ts`**:
```ts
imports: [
  // ... existing ...
  ...(process.env.EXPOSE_TEST_ROUTES === "1" ? [VaultProbeModule] : []),
]
```
This is evaluated at module-load time; the operator must restart the API after flipping the env var.

**`.env.example`**:
```
# Set to "1" to expose test-only routes (used by the integration test suite for
# the 2FA-required guard probe). MUST be unset in production. Default: undefined.
# EXPOSE_TEST_ROUTES=
```

Re-run the spec → all GREEN.

Verify: in a normal `pnpm --filter @simplevault/api start:dev` (no `EXPOSE_TEST_ROUTES`), `curl localhost:3000/vault/_2fa-guard-probe` returns 404. With `EXPOSE_TEST_ROUTES=1 pnpm start:dev`, same call returns 401 (no auth) — proving the route is registered AND the guards are wired.

Production check: `grep -nE "EXPOSE_TEST_ROUTES" apps/api/Dockerfile docker-compose*.yml` should return ZERO (route is not exposed in any production artifact).
  </action>
  <verify>
- `pnpm --filter @simplevault/api build && lint && typecheck` green.
- Spec all 5/5 GREEN.
- Production grep clean.
  </verify>
  <done>
Probe route + guard wired; conditionally registered; tested.
  </done>
</task>

</tasks>

<verification>
- All 5 spec scenarios pass.
- Phase 07 hand-off rule documented (delete the probe + the module as Phase 07's first commit).
- `pnpm --filter @simplevault/api build && lint && typecheck` green.
</verification>

<success_criteria>
- Two atomic commits.
- Truth 15 in INDEX.
- Probe absent from production by env-var convention + verified by grep.
</success_criteria>

<output>
Create `.planning/phases/03-2fa-sessions/03-07-SUMMARY.md`. Document: the conditional-registration pattern (vs `@SetMetadata` + interceptor — picked module-level for clarity); the Phase 07 cleanup checklist; the env-var convention.
</output>
