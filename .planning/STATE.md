# SimpleVault — Project State

> Living memory of where the project is, where it's going next, and what assumptions are open. Updated after each phase completion (or significant change in direction).

---

## Current position

**Stage:** IMPLEMENTATION — **Phase 01 CLOSED 2026-04-29**. **Phase 02 CLOSED 2026-05-02**. **Phase 03 CLOSED 2026-05-04** — all 12/12 plans done + security gate **PASSED** after FINDING-0031 closure re-run. Goal-backward verification 20/20 truths verified (`.planning/phases/03-2fa-sessions/03-VERIFICATION.md`). Four blocking auditors signed off (auth-flow PASS post-closure, crypto PASS-WITH-CONCERNS, owasp-top10 PASS-WITH-CONCERNS, access-control PASS); `threat-modeler` appended `THREAT-MODEL.md` §17.1 (Phase-03 transitions: AT-5 leaf A RESIDUAL → MITIGATED-WITHIN-EPOCH-LATENCY; leaf F RESIDUAL → MITIGATED-FOR-WEBAUTHN-USERS / RESIDUAL-FOR-TOTP-ONLY-USERS; phishing-without-WebAuthn HIGH → MITIGATED-FOR-WEBAUTHN-USERS; new leaf H "TOTP secret extraction from compromised browser" RESIDUAL) + §19 (Phase-03 STRIDE per data flow, 8 entries). FINDING-0031 (HIGH — `revokeOne` did not bump `users.session_epoch`, contradicting Truth 12) FIXED + VERIFIED-CLOSED same gate cycle: `apps/api/src/auth/sessions/session.service.ts:419` now invokes `bumpEpoch(userId)` after the family-revoke UPDATE (load-bearing UPDATE-then-bump order matches `revokeAllForUser`); anti-enumeration preserved (`null` returns occur before any epoch-bump). FINDING-0034 (Info, JSDoc drift on `bumpEpoch` after the fix) closed in same cycle. Tracked tech-debt forwarded to Phase 13: FINDING-0032 (TOTP `/verify` IP-only throttler — add user-keyed ceiling), FINDING-0033 (step-up guard doesn't validate epoch claim), FINDING-0040 (TOTP unwrap should recompute AAD client-side instead of trusting server), FINDING-0050 (webauthn finish-auth status drift 400-vs-401), FINDING-0051 (`ParseUUIDPipe` 400-vs-404 cross-user enumeration), FINDING-0052 (boot-time guard for `EXPOSE_TEST_ROUTES=1 && NODE_ENV=production`), FINDING-0053 (counter-regression doesn't `logger.warn`), FINDING-0054 (pino redact list lacks new field names). Operator checkpoints still pending (NON-blocking for security gate, but recommended before Phase 04 lands UI work that consumes the same surfaces): Plan 10 T4 visual UX review of `/settings/security` + Plan 12 T4 review of new `docs/operator/RUNBOOK.md`. **Phase 02 CLOSED 2026-05-02** — all 12/12 plans done + security gate **PASSED** after FINDING-0010 closure re-run. Goal-backward verification 12/12 truths verified (`.planning/phases/02-auth-crypto/02-VERIFICATION.md`). Five blocking auditors signed off (PASS / PASS-WITH-CONCERNS, no Critical/High open); `threat-modeler` updated `THREAT-MODEL.md` §14–§18 (Phase 02 STRIDE + AT-5). FINDINGS-0011..0029 tracked OPEN as Medium/Low (none block). Tracked tech debt forwarded to Phase 03/13: FINDING-0021 (APP_GUARD ordering disables `/me` user-keyed throttling — falls back to IP) and FINDING-0022 (throttler reads unbounded `req.body.email` pre-Zod) are the most architecturally interesting. See `.planning/phases/02-auth-crypto/02-PHASE-SUMMARY.md` for the cumulative recap and Phase-03+ hand-offs.
**Active phase:** 04 — Personal vault: credentials (PLANNING — `/gsd:plan-phase 4` next).
**Active milestone:** M1 (Personal vault)
**Last commit on planning:** see `git log .planning/`

## Phase status

| Phase | Status |
|---|---|
| 01 — Foundations | **CLOSED 2026-04-29** — all 10 plans complete; security gate PASSED on re-run (gsd-verifier 11/11 + infra-deployment-auditor PASS + dependency-supply-chain-auditor PASS, threat-modeler M0 baseline informational). 9 findings filed, all VERIFIED-CLOSED. Tracked tech debt: two `pnpm.overrides` (multer + lodash) to remove during Phase 02 NestJS 11 upgrade. |
| 02 — Auth + Crypto core | **CLOSED 2026-05-02 — 12/12 plans + security gate PASSED.** `gsd-verifier` 12/12 goal-backward truths verified (`.planning/phases/02-auth-crypto/02-VERIFICATION.md`). Five blocking auditors signed off in `AUDIT-LOG.md`: `crypto-auditor` PASS-WITH-CONCERNS (5 findings: 2M/2L/1info), `auth-flow-auditor` PASS-WITH-CONCERNS after FINDING-0010 re-run closure (initially flagged HIGH; closed by moving `requestId` from error JSON body to `X-Request-Id` response header in `apps/api/src/common/filters/all-exceptions.filter.ts:58` + matching change in `apps/web/src/lib/api/auth-client.ts` + CORS `exposedHeaders` update in `apps/api/src/main.ts:48`; verified-closed re-run section appended to the auditor report; remaining 1 Medium + 2 Low + 2 Info OPEN), `owasp-top10-auditor` PASS-WITH-CONCERNS (1L+1info + concur on FINDING-0010 closure), `input-validation-auditor` PASS-WITH-CONCERNS (2M+3L), `rate-limit-dos-auditor` PASS-WITH-CONCERNS (2M+2L+1info). `threat-modeler` informational pass — appended `THREAT-MODEL.md` §14 (Phase-02 STRIDE per data flow), §15 (AT-5 reframed as session-compromise tree, 7 leaves), §16 (5 load-bearing assertions: pre-auth global salt disclosure ACCEPTED, `__Host-refresh` + same-origin under `pass.runadev.com` MUST hold in prod, AAD per-user binder = SHA256(lower(email)), timing-floor must remain constant-time-Argon2id, family-revoke transactional semantics), §17 (M0 #5 KDF-downgrade + #8 account-enum flipped MITIGATED; three new entries: phishing-without-WebAuthn HIGH for Phase 03, invite hijack via OOB MEDIUM for Phase 07, same-origin invariant breakage LOW now / HIGH if changed), §18 (per-auditor follow-up pointers). FINDINGS open going into Phase 03: most architecturally interesting are **FINDING-0021** (APP_GUARD throttler ordering — `SimpleVaultThrottlerGuard` global guard runs BEFORE `JwtAuthGuard` → `req.user` undefined → REQ-RATELIMIT-006 user-keyed `/me` 100/min silently falls back to IP-keying) and **FINDING-0022** (throttler reads `req.body.email` pre-Zod with no length cap → Redis-flood vector cross-referenced with FINDING-0017 unbounded email column). Decide in Phase 03 whether to fix-now or carry to Phase 13 hardening pass. Plan 12 (E2E Cypress happy + sad + CI e2e job + operator runbook updates) **DONE 2026-04-28**. Plan 11 (web /login + auto-refresh + (authed) route group + /me + logout) **DONE 2026-04-28** — single-step /login form (email + master pw + secret_key) computes `argon2_secret_key_hash = Argon2id(secret_key, server_argon_salt, params)`, POSTs /auth/login, stores access JWT in MEMORY ONLY (`accessTokenStore`), unwraps `master_DEK` + `signing_sk` + `kx_sk` via byte-identical AAD labels matching signup, populates `keyStore`, hard-redirects to /me. Auto-refresh hook fires `POST /auth/refresh` 60 s before exp; on 401/E1005/AUTH_REFRESH_REUSED wipes accessTokenStore + keyStore + redirects to /login (fail-closed); `visibilitychange` listener re-checks freshness on tab return; one 30 s soft retry on transient network errors. (authed) route group is a CLIENT-side guard — server components can't gate on in-memory tokens; AuthProvider mount tries one transparent /auth/refresh from `__Host-refresh` cookie (browser hard-refresh recovery). Logout button on /me ALWAYS wipes locally even on API failure (defence in depth). **LOAD-BEARING decision: `GET /auth/params` extended to also return GLOBAL `serverArgonSalt`** (chicken-and-egg: client needs salt to compute verifier before authenticating; salt is operator-pepper per CRYPTO-STACK §2, not per-user secret; response identical for every caller, anti-enumeration preserved). Same-origin requirement (`__Host-` cookie has no `Domain`) is the prod assumption — Traefik path-routing under `pass.runadev.com` makes prod same-origin by design (documented for 02-12 runbook). Web build green; `node:crypto` regex clean across `.next/static/chunks`. Plan 10 (web /signup multi-step flow) **DONE 2026-04-28** — six-step wizard with reducer-enforced advance gates (invite → master-pw → secret-key reveal+re-paste → 24-word recovery reveal → 4-word challenge confirm → submit-and-derive); secret_key 16 B Crockford-base32, mnemonic via `generateMnemonic`, derivation chain `deriveMasterKek` + `deriveRecoveryKek` + 4 `wrapKey` calls + Ed25519 + X25519 keypairs; 10-field signup envelope matches 02-07 byte-for-byte; AAD per-user binder = `SHA256(lower(email))` (decision LOAD-BEARING for 02-11 login unwrap — same binder must be re-derived); per-blob AAD label prefixes `"sv:user-master:v1\|"` / `"sv:user-recovery:v1\|"` / `"sv:user-sign-sk:v1\|"` / `"sv:user-kx-sk:v1\|"` (FROZEN). NO auto-login on success — redirects to `/login?signed_up=1`. In-memory key-store with `wipe()` zero-overwrites every Uint8Array on flow end / unmount / error. Ed25519 keypair generation backfilled into `@simplevault/crypto` (deferred from 02-04) as a sibling `sign.ts` exposed in BOTH browser + node barrels (`generateSigningKeyPair`); parity test snapshot updated; total crypto suite is now 71 tests. CSP gained `'wasm-unsafe-eval'` (libsodium WASM compilation) + `connect-src` widening to the API origin. `/signup` chunk size 431 kB (libsodium WASM dominant); `node:crypto` regex clean across every `.next/static/chunks/**/*.js`. Plan 09 (GET /me + audit-events + throttler + Pino redaction extension) **DONE 2026-04-30** — `/me` body shape locked at `{id,email,createdAt,argon2Params}.strict()` for 02-11/04-xx; `AuditEvent` v1 shape FROZEN for Phase 10 hash-chain (`{v:1, ts, action, actorUserId, targetId, outcome, reason?, ipHashB64?, uaFamily?, requestId?, data?}` + 11-action enum); `@nestjs/throttler@6.5` + `@nest-lab/throttler-storage-redis@1.2` replace the in-memory limiter (deleted), 9 named ceilings (signup 3/IP/h, login 5/IP/min + 10/email/h, refresh 60/IP/min, /me 100/user/min user-keyed); `SimpleVaultThrottlerGuard` fails open on Redis outage with warn-log + sets `Retry-After`. Pino redaction comprehensive (load-bearing reference for auth-flow-auditor). Plan 01 (NestJS 11 upgrade) **DONE 2026-04-29**. Plan 02 (Argon2id + AEAD + calibrate) **DONE 2026-04-29**. Plan 05 (DB schema) **DONE 2026-04-28**. Plan 03 (BIP-39 + key hierarchy + recovery wrap) **DONE 2026-04-28**. Plan 04 (X25519 sealed-box + symbol-parity + node-barrel tighten) **DONE 2026-04-28** — TDD, 16 new tests across 2 new suites (69 total in `packages/crypto`), browser bundle purity grep clean. Public API: `generateKxKeyPair`/`sealedBoxSeal`/`sealedBoxOpen` + `KxKeyPair`/`SealedBoxKeyLengthError`. `node.ts` switched to explicit named re-exports — server barrel now strictly excludes `deriveMasterKek`/`deriveRecoveryKek`/all bip39 helpers (functions taking password/secret_key/mnemonic), giving the server-side-never-sees-secrets invariant type-level enforcement. `index.ts` adds types-only re-exports for `@simplevault/crypto/types` consumers. Plan 06 (operator CLI) unblocked. Plan 07 (signup) needs Ed25519 keypair generation (deferred to 02-07 itself) plus the wrap calls documented in 02-04-SUMMARY. |
| 03 — 2FA + sessions | **CLOSED 2026-05-04 — 12/12 plans + security gate PASSED.** `gsd-verifier` 20/20 goal-backward truths verified. Four blocking auditors signed off in `AUDIT-LOG.md`: `auth-flow-auditor` PASS post-closure of FINDING-0031 (HIGH; `revokeOne` did not bump `session_epoch` — fixed by adding `bumpEpoch(userId)` after family-revoke at `apps/api/src/auth/sessions/session.service.ts:419`; load-bearing UPDATE-then-bump order matches `revokeAllForUser`; anti-enumeration preserved; doc-drift FINDING-0034 closed same cycle), `crypto-auditor` PASS-WITH-CONCERNS (1L+2I), `owasp-top10-auditor` PASS-WITH-CONCERNS (1L+4I), `access-control-auditor` PASS (0 findings). `threat-modeler` informational pass — appended `THREAT-MODEL.md` §17.1 + §19. FINDINGS open going into Phase 04: 1 Med (0032) + 3 Low (0033, 0040, 0052) + 5 Info (0041, 0042, 0050, 0051, 0053, 0054) — none block, all forwarded to Phase 13 hardening. Operator checkpoints pending (NON-blocking): Plan 10 T4 visual UX + Plan 12 T4 runbook review. **2 operator checkpoints pending (Plan 10 T4 UX visual + Plan 12 T4 runbook review).** Waves 1–7 complete. Wave 7 closed 2026-05-04 by Plan 03-12 (`b74ab0e` docs T3 + `acbdc42` T1+T2 specs/helpers/CI) — new docs/operator/RUNBOOK.md (day-2 ops gap filled), SECURITY-NOTES Phase-03 invariants section, EXPOSE_TEST_ROUTES-gated test-helpers controller, 2 LIVE Cypress specs (sessions + 2fa-removal) + 2 SKIPPED stubs with detailed deferral comments. Wave 6 closed 2026-05-04 by Plans 03-10 (`e2e8905`/`df46767`/`e18821d`) and 03-11 (`7affc1e`/`2bf83f4`) — full web 2FA UX shipped: `/settings/security` (passkey + TOTP enrol + phishing-warning copy), `/settings/sessions` (list + revoke + revoke-all), `/login/2fa` step-up consumer (passkey + TOTP). One new API endpoint `GET /2fa/step-up-material` (step-up guarded; folds wrapped TOTP secrets + unwrap material in one roundtrip — Rule-3 architectural deviation documented in 03-10-SUMMARY). Wave 5 closed 2026-05-04 by Plan 03-09 (`859c7e4`/`8e3215c`/`a4283a0`) — `JwtAuthGuard` is now the global APP_GUARD before `SimpleVaultThrottlerGuard` with `@Public()` opt-out (10 routes); `login-email` throttler keys via `sha256(email).slice(0,16)`. **FINDING-0021 + FINDING-0022 → FIXED-PENDING-VERIFICATION**. Wave 4 closed 2026-05-02 by Plan 03-08 (`73cef47` API + `713a5fb` web). Wave 3 closed by recovery agent 2026-05-02 (Plans 03-05 sessions API, 03-06 2FA methods API, 03-07 vault-2FA-required guard + EXPOSE_TEST_ROUTES probe). 24/24 api tests green. See `.planning/phases/03-2fa-sessions/03-{05,06,07}-SUMMARY.md`. Plan 02 (WebAuthn API + step-up JWT signer + dual-guard purpose discriminator) **DONE 2026-05-02** — `@simplewebauthn/server@^11` + `@simplewebauthn/types@^11` pinned; `StepUpJwtService` signs `{sub, purpose:"2fa-stepup", epoch}` with same `JWT_SECRET` as access token (TTL 120s, env `STEP_UP_TOKEN_TTL`); `JwtAuthGuard` extended with `decodeJwt` pre-check refusing any token where `payload.purpose !== undefined`; `Require2FAStepUpGuard` is the dual that ONLY accepts `purpose:"2fa-stepup"`. `/2fa/webauthn/{begin,finish}-register` (auth-required) + `/2fa/webauthn/{begin,finish}-auth` (step-up-required): atomic challenge consume `DELETE FROM webauthn_challenges WHERE user_id=$1 AND kind=$2 RETURNING challenge, expires_at` (Key Link 2 — single statement, no TOCTOU); `expectedRPID` and `expectedOrigin` passed explicitly to `verifyRegistrationResponse` + `verifyAuthenticationResponse` (Key Link 9 — defaults are unsafe per @simplewebauthn v11); counter-regression rule `stored>0 && new<=stored → 401 + audit reason="counter_regression"` (the `stored>0` exception preserves never-incrementing-authenticator path for iCloud Keychain); operator-decision env vars baked in (`WEBAUTHN_RP_ID`, `WEBAUTHN_RP_NAME`, `WEBAUTHN_ORIGIN` — fail-fast at boot if `NODE_ENV=production` AND any unset; dev fallbacks `localhost`/`http://localhost:3000`); `attestationType=none`, `userVerification=required`, `residentKey=preferred`, `pubKeyCredParams=[-7,-257]`. New error codes E1011..E1014; new audit actions `auth.2fa.webauthn.{register,auth}.{ok,fail}`; new throttler ceilings `2fa-register-user` (10/min user-keyed) + `2fa-webauthn-auth-ip` (30/min IP-keyed). Three atomic commits (`5ae823d`, `d90459f`, `3f8dfec`). Several Rule-3 cross-plan unblock fixes documented in SUMMARY (Plan-04's required `epoch` claim broke the build → added `epoch:0` stub in login + refresh sites, subsequently overwritten by Plan 04's `getEpoch` hop; Plan-03's TOTP service had unused-`logger` lint blocker → added `void this.logger;` constructor reference; @simplewebauthn JSON-envelope types live in sibling `@simplewebauthn/types` package, not `server` — added as direct dep). Plan 03 (TOTP API + browser-only crypto) **DONE 2026-05-02** — RFC 6238 helpers (`computeTotpStep` / `verifyTotpCandidate` / `buildOtpauthUrl` + `totpReady`) browser-only via `@simplevault/crypto/browser` (parity-test snapshot enforces; node barrel cannot import them); HMAC-SHA-1 backend via `@noble/hashes` because libsodium-wrappers-sumo intentionally drops `crypto_auth_hmacsha1` (project's only SHA-1 use, documented as RFC-mandated). Server endpoints `/2fa/totp/{begin,finish}-register` + `/verify` ship with: 32-byte issuance-nonce + Redis `SET ... EX 120 NX` + `GETDEL` for begin→finish atomicity (sha256(nonce) as key, never raw token); INSERT seeds `last_used_step = candidateStep` so the registration code is also burned at /verify (anti-replay-of-enrolment-POST); atomic CAS replay guard `UPDATE totp_credentials SET last_used_step = $cs, last_used_at = now() WHERE id = $cid AND user_id = $uid AND last_used_step < $cs RETURNING id` (zero rows → 401 `AUTH_2FA_TOTP_REPLAY` E1015 — anti-enumeration: same uniform 401 for wrong cred / wrong user / replay); `/verify` mints full session (epoch claim from `users.session_epoch`) with the same exit shape as `/auth/login` 1FA-only success. Server-side grep clean: zero references to `master_DEK` / `master_KEK` / `computeTotpStep` / `verifyTotpCandidate` in `apps/api/src/twofa/totp/` (one comment asserting the invariant). 18 new RFC-6238-Appendix-B vector tests in `packages/crypto/test/totp.test.ts` (91 total crypto tests green). Two parallel-execution Rule-3 deviations: T1 RED files were swept into Plan 02-T1's commit (`645c00f`); audit-events / throttler-config / error-codes edits swept into Plan 02-T2's commit (`d90459f`) — content correct in HEAD, attribution split across plans 02 + 03. Two file-renames: plan-prescribed `AUTH_2FA_TOTP_REPLAY = E1011` collided with Plan 02's claim → assigned E1015; new error code E1016 `AUTH_2FA_TOTP_ISSUANCE_INVALID` for the begin→finish nonce gate. New rate-limit ceiling `twoFaVerifyIp` (30/min IP-keyed). See `.planning/phases/03-2fa-sessions/03-03-SUMMARY.md`. Plan 04 (session-epoch JWT claim + JwtAuthGuard epoch check) **DONE 2026-05-02** — see `.planning/phases/03-2fa-sessions/03-04-SUMMARY.md`. Wave 1: Plan 01 (DB schema foundation: webauthn_credentials + webauthn_challenges + totp_credentials + users.session_epoch + email varchar(254) — FINDING-0017+0022 storage tier fold) **DONE 2026-05-02** — three atomic commits (`bf642c5`, `33b97ba`, `6549e9a`); single Drizzle migration `0002_phase03_2fa_sessions.sql` carries the three new tables, the `session_epoch` column, AND the email type tightening (DO-block pre-flight guard prepended; `USING substring(email,1,254)` cast for idempotency). Zod tier capped: `LoginSchema.email`, `MeResponseSchema.email`, CLI invite-create email arg all `.max(254)`. `SignupSchema`/`InviteRedeemSchema` had no email field (server takes it from the locked invite row) — plan-listed but n/a. Throttler-tier fix (sha256(email).slice(0,16) keying — closes FINDING-0022 fully) deferred to Plan 03-09 per INDEX disposition. Migration runtime apply on `postgres:18.3-alpine` deferred to Plan 12 e2e CI job (no live PG this session); migration is structurally valid (drizzle-kit `No schema changes` after merge — snapshot integrity verified). Builds green: `pnpm --filter @simplevault/{db,api,web,cli,shared} build`. 4 small auto-deviations (Rule 1/2/3): Drizzle Kit emits fresh 0003 instead of extending 0002 (manually merged + chain repointed), Drizzle Kit's random-word migration tag renamed to plan name, unused `text` import dropped after switching to `varchar`, `MeResponseSchema.email` capped as Rule 2 defence-in-depth. Two plan-listed sites n/a (no email field). No Rule 4 deviations. Wave 2 (parallel: Plans 02 WebAuthn API / 03 TOTP API / 04 session-epoch JWT claim) UNBLOCKED. See `.planning/phases/03-2fa-sessions/03-01-SUMMARY.md`. |
| 04 — Personal vault: credentials | PLANNED |
| 05 — Personal vault: pages | PLANNED |
| 06 — Page double-lock | PLANNED |
| 07 — Shared vaults: create + invite | PLANNED |
| 08 — Shared vaults: members + revoke | PLANNED |
| 09 — Unanimous delete + override | PLANNED |
| 10 — Audit log + hash chain | PLANNED |
| 11 — Recovery + export | PLANNED |
| 12 — Web hardening | PLANNED |
| 13 — Security Hardening (gate) | PLANNED |
| 14 — Production deploy | PLANNED |

## Configuration

- Mode: YOLO (auto-approve)
- Depth: Comprehensive (14 phases across 8 milestones)
- Parallelization: enabled (multiple plans within a phase run in parallel)
- Research: completed (4 outputs in `.planning/research/`)

## Load-bearing decisions (changing any of these requires re-threat-model)

1. **Two-secret model** (1Password-style: master password + 128-bit secret_key). UPGRADED from initial single-secret draft based on `crypto-auditor` research. Operator should explicitly review REQ-CRYPTO-003 and confirm or veto before Phase 02 begins.
2. E2E client-side encryption with XChaCha20-Poly1305 + Argon2id (OWASP 2024 params, calibrated to 750 ms).
3. BIP-39 24-word recovery phrase, no server-side recovery.
4. WebAuthn + TOTP 2FA, mandatory before vault sharing.
5. HMAC hash-chain audit log + Ed25519-signed daily Merkle checkpoints off-machine.
6. Self-hosted Docker + VPS with Caddy reverse proxy.
7. Drizzle ORM (SQL-first, auditable queries).
8. TipTap rich-text editor with strict schema, no images/tables in v1.
9. Unanimous deletion with 30d timeout + 7d notice + owner override.
10. Per-user X25519 sealed-box key wrapping for shared vaults.

## Operator deployment target (CONFIRMED 2026-04-28)

- **Platform:** Dokploy on existing VPS (Traefik-based PaaS over Docker)
- **Domain:** `pass.runadev.com` — operator must add A record to VPS IP; Dokploy + Traefik provisions Let's Encrypt TLS automatically
- **Postgres:** managed by Dokploy, **PostgreSQL 18.3** (bleeding-edge — verify Drizzle Kit + `pg` driver compatibility in Phase 01; expected OK since 18 doesn't change wire protocol)
- **Redis:** fresh instance to be created in Dokploy for SimpleVault
- **Backups:** VPS-level snapshots already exist (operator's hosting provider). **STILL NEED app-level logical backups** — VPS snapshots can be corrupted with the data. Add lightweight `pg_dump` cron service to nightly-dump Postgres logical backup to a separate location (S3 / off-site rsync). NOT shipping `restic` sidecar — replaced by simpler `pg_dump` cron.
- **Reverse proxy:** Traefik (Dokploy-managed) — **Caddy is OUT**. Security headers move to app layer (Next.js middleware + NestJS helmet) so they don't depend on the proxy.
- **Migrations:** simpler pattern — Dockerfile `prestart` hook runs `drizzle-kit migrate` before `node dist/main.js` (vs separate init container). Less Dokploy friction.
- **Secrets:** stored in Dokploy's encrypted env-var UI (NOT in `.env` files in git).

## Open questions / pending decisions

- ~~REQ-CRYPTO-003 confirmation~~ — **CONFIRMED 2026-04-28: two-secret model (1Password-style) is GO.** Signup generates a 128-bit secret_key shown once in an Emergency Kit. Server stores only `Argon2id(secret_key, server_salt)` for verification. New-device logins require re-entering the secret_key (can be persisted device-locally encrypted afterwards).
- **Audit checkpoint git repo**: where to host the off-machine `audit-checkpoints` git repo (separate VPS? GitHub private? cold storage)? Decide before Phase 10.
- **SMTP provider**: which SMTP for invite emails (Postmark? Mailgun? self-hosted Postfix relay)? Decide before Phase 02 (signup) or Phase 07 (sharing) at latest.
- **Operator's own 2FA**: should the operator account have stricter requirements than regular users (mandatory hardware key)? Decide before Phase 14.
- **Off-site location for `pg_dump`** (CONFIRMED 2026-04-28): rsync over SSH to operator-owned VPS/NAS. Before Phase 14, operator must provide: target host + SSH user + target path; backup container will need a dedicated SSH keypair (private key stored as Dokploy secret, public key in target's `authorized_keys` with `command=` restriction to a `rrsync`-style read-only-append wrapper for blast-radius reduction).

## Things to remember

- Plans for each phase live under `.planning/phases/XX-name/` and are generated by `/gsd:plan-phase N`.
- Every phase has a security gate; auditors are run via `/gsd:verify-work N`. Findings tracked in `.planning/security/FINDINGS.md`.
- `threat-modeler` agent runs at the start of each milestone, not each phase.
- Phase 13 is the consolidated security re-pass; auditors also run inline at every prior phase gate.
- Phase 14 (production deploy) requires operator explicit sign-off on the live URL.

## Next action

**Phase 03 IN PROGRESS — 12/12 plans done; two operator checkpoints pending (Plan 10 T4 UX visual + Plan 12 T4 runbook review). After both confirm, run `/gsd:verify-work 3`.** Plan 03-01 (DB schema foundation) **DONE 2026-05-02** (`bf642c5`, `33b97ba`, `6549e9a`). Plan 03-02 (WebAuthn API + step-up JWT) **DONE 2026-05-02** (`5ae823d` T1 step-up signer + Require2FAStepUpGuard + JwtAuthGuard purpose-claim rejection, `d90459f` T2 begin/finish-register + @simplewebauthn/server@^11 + @simplewebauthn/types@^11, `3f8dfec` T3 begin/finish-auth + counter-regression rule with stored>0 exception). Plan 03-03 (TOTP API + browser-only crypto) **DONE 2026-05-02**. Plan 03-04 (session-epoch JWT claim + JwtAuthGuard epoch check) **DONE 2026-05-02** (`4adfc6f` T1 RED, `aae6636` T2 GREEN, `b3bc306` T3 wire-up): `JwtService.AccessTokenClaims` gains `epoch: number`; new `SessionEpochCache` (Redis-backed per-user epoch cache, TTL 60s default via `SESSION_EPOCH_CACHE_TTL`, in-flight Map de-dup, bustGen race-guard for the read-then-bump-then-set worst case); `JwtAuthGuard` injects the cache and 401s with `AUTH_SESSION_REVOKED` (E1017) on epoch mismatch; `SessionService` gains `getEpoch` + `bumpEpoch` (UPDATE-then-DEL ordering); login + refresh sign with `epoch: await sessions.getEpoch(userId)`. **Closes Phase-02 deferred REQ-AUTH-004** (instant access-token revocation) and **AT-5 leaf A residual** → MITIGATED-WITHIN-EPOCH-LATENCY. First vitest target inside `apps/api` (8 invariants, mock-based — real PG/Redis e2e in Plan 12). Plan-listed twofa stub-replacement sites are owned by Plans 02/03 (parallel Wave 2 work, not yet committed) — left for those plans' close-out per "touch only files in your block" rule.

**Wave 2 COMPLETE.** All three Wave 2 plans (03-02 WebAuthn API, 03-03 TOTP API, 03-04 session-epoch JWT claim) closed with atomic commits. Wave 3 (Plans 03-05 sessions endpoints / 03-06 2FA methods / 03-07 vault.* require-2FA guard) UNBLOCKED.

**Wave 3 COMPLETE (recovery agent, 2026-05-02).** All three Wave 3 plans closed with atomic commits after a sibling-agent stall left interleaved hunks across `app.module.ts` / `audit-events.ts` / `throttler.config.ts` / `twofa.module.ts` / `packages/shared/src/zod/index.ts` / `.env.example` / `apps/api/test/2fa-required-guard.spec.ts`. Recovery agent attributed each hunk to its owning plan via hand-written patches (`git apply --cached`) and committed sequentially: Plan 03-05 (`edb35e8` T1 GET /sessions + DELETE /sessions/:id, `760ca16` T2 POST /sessions/revoke-all + bumpEpoch + cookie-clear, `6ea12cf` docs) — sessions API surface distinct from `auth/sessions/SessionService` (refresh-rotation primitive); `DISTINCT ON (family_id)` collapses rotation chain to one representative row per family; revoke-one does NOT bump epoch (revoke-all does — Plan 04 Key Link 3); 404-NOT-403 anti-enumeration on cross-user; new ceilings `sessions-list-user` (60/min) / `sessions-revoke-user` (30/min) / `sessions-revoke-all-user` (5/min) — IP-keyed fallback until Plan 09. Plan 03-06 (`a8b31f1` T1 GET /2fa/methods + DELETE /2fa/methods/:id, `5de3538` T2 removal-guard + integration test, `98a5427` docs) — strict-allowlist response (Zod re-parse defence-in-depth, NEVER returns secret material), cross-table delete (webauthn_credentials ⊕ totp_credentials) with 404 anti-enumeration, `userHasSharedVaultDependency` Phase-07 hand-off seam (mutable `MethodsService.sharedVaultDependencyCheck` field, default-false stub, integration test flips to `() => true` to verify 409 path), new error code `AUTH_2FA_REMOVAL_BLOCKED = E1018` (E1014 was taken — recovery agent renumbered), audit `auth.2fa.method.removed` (namespace normalisation), new ceilings `2fa-methods-list-user` (60/min) / `2fa-methods-delete-user` (30/min). Plan 03-07 (`d2f581e` T1 RED pre-existing, `2b280cf` T2 GREEN — Require2FAGuard + DbBackedRequire2FACountReader + REQUIRE_2FA_COUNT_READER injection token, TwoFaGuardProbeController, VaultProbeModule, conditional `...(process.env.EXPOSE_TEST_ROUTES === "1" ? [VaultProbeModule] : [])` spread in app.module.ts; spec body rewritten away from dynamic-import-of-app.module to source-grep + production-artifact-grep + runtime-predicate-eval triad — orthogonal coverage that doesn't depend on parallel-Wave-3 wiring; production grep clean for `EXPOSE_TEST_ROUTES`). Final HEAD api test suite: 24/24 GREEN (jwt-epoch 8 + 2fa-removal 5 + 2fa-required-guard 11). Build green. Wave 4 (Plan 08 — `/auth/login` branches on 2FA presence) UNBLOCKED. Several Rule-3 deviations across all three plans (Wave-3 cross-talk attribution, error-code slot collision E1014→E1018, audit-action namespace normalisation, file-location overrule for Require2FAGuard) all documented in plans' SUMMARY files.

**Wave 7 COMPLETE (T1–T3, 2026-05-04). Plan 03-12 T4 (operator runbook review) PENDING.** Plan 03-12 (E2E Cypress + operator docs) closed with two atomic commits — `b74ab0e` T3 and `acbdc42` T1+T2. **T3 docs**: `docs/operator/RUNBOOK.md` (NEW file, the day-2 ops gap that the pre-existing CLI/DOKPLOY-DEPLOY/LOCAL-DEV/SECURITY-NOTES quartet didn't cover) — Phase 03 env-var matrix (WEBAUTHN_RP_ID/RP_NAME/ORIGIN, STEP_UP_TOKEN_TTL, SESSION_EPOCH_CACHE_TTL, EXPOSE_TEST_ROUTES with prod values + load-bearing notes); lost-2FA recovery procedure (operator deletes 2FA rows + bumps session_epoch + busts Redis cache via psql + redis-cli inside the Dokploy api container; recovery phrase explicitly does NOT bypass 2FA — load-bearing per threat model); WebAuthn RP-ID change procedure (credential-bricking event with 7-day notice + bulk webauthn_credentials delete + bulk session_epoch bump); EXPOSE_TEST_ROUTES production safety verification (3 grep + Dokploy-panel + container-env-grep checks the operator runs before every deploy); operator-initiated single-user / all-users session revocation procedures with the load-bearing "always pair the bump with a Redis cache DEL" note. `docs/operator/SECURITY-NOTES.md` extended with "Phase 03 invariants" section the auditor reads at the gate (TOTP secret browser-only + server-side grep verification; AAD label scheme `sv:<scope>:v1|`; session-epoch column + Redis cache + bust semantics; WebAuthn RP-ID load-bearing pointer to RUNBOOK; complete `@Public()` route allow-list — every route NOT on this list MUST require an access JWT, auditor cross-check; 2FA-required guard hand-off seam to Phase 07; 12 audit-action enum extensions FROZEN for Phase 10's hash-chain indexing; findings disposition table — FINDING-0017 closed by Plan 03-01, FINDING-0021 + FINDING-0022 fixed-pending-verification by Plan 03-09, FINDING-0011 deferred to Phase 13). `.env.example` adds `SESSION_EPOCH_CACHE_TTL=60` (Plan 04 landed the env-var read but didn't update the example). **T1+T2 specs + helpers + CI**: `apps/api/src/test-helpers/{test-helpers.controller.ts, test-helpers.module.ts}` — three EXPOSE_TEST_ROUTES-gated POST routes (`flip-shared-vault-stub {value: bool}`, `seed-totp-credential {email, name} → {id}`, `mutate-webauthn-counter {credentialId, counter}`) mirroring the VaultProbeModule pattern from Plan 03-07. `apps/web/cypress/e2e/sessions.cy.ts` — covers Truths 11 + 13 (current session pinned, empty-other-sessions state, revoke-all wipes local + redirects); Truth 12 (revoke a sibling session) deferred to live-iteration (multi-session setup needs cy.session machinery or a new cy.task). `apps/web/cypress/e2e/2fa-removal.cy.ts` — covers Truth 10 + the Phase-07 hand-off seam end-to-end: signup → login → cy.request seed-totp-credential → cy.request flip-shared-vault-stub:true → visit /settings/security → click Remove → assert AUTH_2FA_REMOVAL_BLOCKED 409 surfaces in DOM as the forward-looking shared-vault copy → reset stub. **`apps/web/cypress/e2e/2fa-webauthn.cy.ts` and `2fa-totp.cy.ts` are DESCRIBE.SKIP'd** with detailed comment blocks documenting the live-iteration owed: WebAuthn needs CDP virtual-authenticator wiring in `setupNodeEvents` (Cypress 14 doesn't ship a stable wrapper; current config uses cy.exec not cy.task); TOTP needs intercepting the browser-side secret generation (operator decision: cy.stub `sodium.randombytes_buf` vs test-only `window.__SV_TEST_TOTP_SECRET__` seam when EXPOSE_TEST_ROUTES === "1"). Status table in 03-12-SUMMARY makes the coverage gaps explicit for the auditor. `.github/workflows/ci.yml`: rename e2e job to mention Phase 03 specs + add `EXPOSE_TEST_ROUTES: "1"` to the api env block (CI runs `cypress:run` against the `cypress/e2e/**/*.cy.ts` glob — new specs auto-included). API typecheck + build + 32/32 vitest specs all green. Cypress live verification owed to the next CI push. **T4 (operator runbook review) PENDING** — operator owes a recorded decision in 03-12-SUMMARY § Operator review on the "no recovery phrase bypass" rule (acceptable indefinitely vs Phase-11 revisit). **Next step: `/gsd:verify-work 3`** — runs the 4-auditor gate (auth-flow + crypto + owasp-top10 + access-control) + threat-modeler informational pass against this surface. See `.planning/phases/03-2fa-sessions/03-12-SUMMARY.md`.

**Wave 6 COMPLETE (T1–T3, 2026-05-04). Plan 03-10 T4 (operator UX checkpoint) PENDING.** Plans 03-10 (web /settings/security 2FA enrol + /login/2fa step-up consumer) and 03-11 (web /settings/sessions list + revoke + revoke-all) closed in parallel. Plan 03-11: `7affc1e` T1 `apps/web/src/app/(authed)/settings/sessions/{page,session-list,revoke-button}.tsx` + `apps/web/src/lib/api/sessions-client.ts` (mirrors auth-client patterns; reuses the shared `request` helper exported from auth-client.ts in this commit so twofa-client + sessions-client + auth-client all share one fetch+Zod+error-envelope path); current row pinned + visually distinct via emerald-accent panel; `Intl.RelativeTimeFormat` for "created/last used" timestamps (avoids the date-fns ~40 KB dep); 404 surfaces as generic "couldn't revoke" preserving server-side anti-enumeration; `2bf83f4` T2 `revoke-all-button.tsx` confirm-then-execute pattern wiping `keyStore + accessTokenStore` then hard-redirecting to /login (mirrors auth-context.tsx Phase-02 logout defence-in-depth wipe-on-failure); cross-tab implication documented inline (revoke-all bumps session_epoch → other tabs lose access within ≤60s SessionEpochCache TTL). Plan 03-10: `e2e8905` T1 `apps/web/src/app/(authed)/settings/security/{page,method-list}.tsx` + `apps/web/src/lib/api/twofa-client.ts`; passkey panel emerald-bordered + "Recommended — phishing-resistant" badge as PRIMARY CTA, TOTP panel zinc + amber-warning panel ABOVE the enrol area carrying the verbatim THREAT-MODEL §17 copy "Authenticator-app codes can be entered into a phishing site. Use a passkey when possible — passkeys cryptographically refuse to log in to a fake site."; method list shows PASSKEY (emerald) vs AUTHENTICATOR APP (amber) badges + Remove button surfacing the Phase-07-aware AUTH_2FA_REMOVAL_BLOCKED 409 with forward-looking shared-vault copy. `df46767` T2 `enroll-passkey-button.tsx` via `@simplewebauthn/browser@^11` (pinned to match `@simplewebauthn/server@^11` server-side); NotAllowedError → graceful "Cancelled" status; bundle delta /settings/security 127 → 130 kB First-Load (+3 kB gzipped). `e18821d` T3 — three sub-deliverables: (a) **NEW API endpoint** `GET /2fa/step-up-material` (StepUpMaterialController step-up guarded; returns `{userArgonSalt, argon2Params, wrappedMasterDek, totpCredentials[]}` — folds the wrapped TOTP secrets AND the unwrap material into one roundtrip; the plan called out only `GET /2fa/totp/credentials` but completing TOTP step-up client-side ALSO needs `userArgonSalt + argon2Params + wrappedMasterDek` since the 2FA-required login response per Plan 08 carries no wrapped material; **Rule-3 architectural deviation** documented inline + in 03-10-SUMMARY); (b) **TOTP enrolment UI** `enroll-totp-flow.tsx` + `apps/web/src/lib/crypto/totp-wrap.ts` + `AAD_LABEL_TOTP = "sv:user-totp:v1|"`; flow: begin-register (server reserves issuance nonce TTL 120s) → 20-byte secret via libsodium → buildOtpauthUrl + qrcode-generated QR → user enters code → `verifyTotpCandidate` ±1 step locally → wrap secret under master_DEK with `encodeAad(argon2Params, AAD_LABEL_TOTP || SHA256(lower(email)))` → finish-register; best-effort secret zeroing on every transition; (c) **/login/2fa step-up consumer** `apps/web/src/app/login/2fa/page.tsx` + `apps/web/src/lib/auth/step-up-flow.ts` (completeWithPasskey + loadStepUpMaterial + findTotpMatch + completeWithTotp + applyStepUpSession primitives); reads stash from sessionStorage; passkey path runs WebAuthn ceremony directly; TOTP path GETs step-up material → derives master_KEK locally from keyStore-preserved password+secret_key → unwraps master_DEK → walks ALL user TOTP credentials × ±1-step drift to identify which credential matches the typed code → POSTs verify; on success applyStepUpSession runs the same `unlockSecrets` path as 1FA login; on missing handoff/expired step-up wipes everything + bounces to /login. **/login/page.tsx** switched from `window.location.assign("/login/2fa")` to soft `router.push("/login/2fa")` so the JS realm survives — keyStore singleton preserves password+secret_key+email across navigation (stashed under `step_up_email/step_up_password/step_up_secret_key` keys); 1FA-only branch unchanged byte-for-byte. **Server-side grep clean** for master_DEK / computeTotpStep / verifyTotpCandidate in apps/api/src/twofa/totp/ — server only round-trips opaque bytes (Plan 03 Key Link 3). Two new web deps: `@simplewebauthn/browser@^11`, `qrcode + @types/qrcode`. Bundle deltas: `/settings/security` 548 kB First-Load, `/login/2fa` 538 kB (libsodium dominates — already loaded by /login + /signup, Webpack shares the chunk; per-route number is Next's First-Load report). API + web typecheck + build green; 32/32 vitest specs green. **T4 (operator UX checkpoint) PENDING** — code-side verification clean (grep confirms passkey panel above TOTP panel with "Recommended — phishing-resistant" badge; amber warning panel above TOTP enrol button carrying the verbatim phishing copy); operator owes a screenshot at `.planning/phases/03-2fa-sessions/03-10-uxcheck.png` confirming visual rendering. Wave 7 (Plan 03-12 — Cypress 2fa-webauthn + 2fa-totp + sessions specs + runbook) UNBLOCKED. See `.planning/phases/03-2fa-sessions/03-10-SUMMARY.md` and `03-11-SUMMARY.md`.

**Wave 5 COMPLETE (2026-05-04).** Plan 03-09 (throttler ordering fix + login-email keying — FINDING-0021 + 0022 closure) closed with three atomic commits: `859c7e4` T1 `@Public()` decorator + `IS_PUBLIC_KEY` metadata + `JwtAuthGuard` Reflector dependency (`getAllAndOverride([handler, class])` short-circuits to `true` BEFORE bearer-token inspection — zero auth overhead on public routes); `8e3215c` T2 `JwtAuthGuard` registered as APP_GUARD in `app.module.ts` BEFORE `SimpleVaultThrottlerGuard` (registration ORDER LOAD-BEARING per NestJS APP_GUARD semantics — second guard fires only if first allowed) + `@Public()` applied to 10 routes (`/health`, `/invite/redeem`, `/auth/{signup,login,refresh,logout,params}`, plus `/2fa/webauthn/{begin,finish}-auth` and `/2fa/totp/verify` which authenticate via `Require2FAStepUpGuard` instead of `JwtAuthGuard` — their step-up tokens carry `purpose:"2fa-stepup"` which `JwtAuthGuard` rejects via the Plan-02 pre-decode discriminator); `a4283a0` T3 `login-email` throttler keying via `createHash("sha256").update(email).digest("hex").slice(0,16)` (16-char fixed length tracker — bounds Redis key memory under arbitrary-length email floods, removes lowercased-email PII from Redis keys; same email always hashes the same way so existing 10/email/h semantics preserved) + header comment block on `throttler.config.ts` documenting the load-bearing invariant that the throttler runs BEFORE Zod validation (guards before pipes), so any future ceiling keyed off `req.body` MUST hash + slice. Pre-existing per-controller `@UseGuards(JwtAuthGuard)` decorators kept as harmless defence-in-depth (the second invocation is a Reflector hit + a SessionEpochCache Redis hit — ~1ms). HEAD api test suite still 32/32 GREEN (jwt-epoch.spec.ts updated for the new 3-arg `JwtAuthGuard` constructor signature; `Reflector` injected via vanilla `new Reflector()`, mock `ExecutionContext` exposes `getHandler` + `getClass` returning marker functions without `IS_PUBLIC_KEY` metadata so the auth path runs). Build green. Truths 18 + 19 verified at the code layer; live e2e reproduction (101 `/me` from IP-1 then one from IP-2 with the same JWT must 429; `redis-cli --scan --pattern 'login-email:em:*'` after a 4KB-email flood must return fixed 16-hex keys) deferred to `/gsd:verify-work 3` and Plan 12's Cypress run — the unit suite verifies the guard contract; the live test verifies the APP_GUARD framework wiring. **FINDING-0021 + FINDING-0022 → FIXED-PENDING-VERIFICATION** (both Medium). Wave 6 (Plans 10 web `/settings/security` + 11 web `/settings/sessions`, parallel) UNBLOCKED. See `.planning/phases/03-2fa-sessions/03-09-SUMMARY.md`.

**Wave 4 COMPLETE (2026-05-02).** Plan 03-08 (`/auth/login` branches on 2FA presence) closed with two atomic commits: `73cef47` T1 API + integration spec, `713a5fb` T2 web client. `LoginService` returns a discriminated `LoginResult = LoginOk | LoginStepUp` after the constant-time compare passes; `methods.countActive(user.id) >= 1` mints a step-up token via `StepUpJwtService`; otherwise the Phase-02 session-mint code runs unchanged. Truth 8 verified — no `kind` field on the 1FA-only branch (Phase-02 byte-equal regression-free); `kind:"2fa-required"` discriminator on the new branch. Audit dual-emit (`auth.login.ok` with `data.kind:"session"` + `auth.login.step_up_issued` with `data.kind:"step-up"`) gives operator dashboard two orthogonal forensic axes. Anti-enumeration intact via post-1FA branching: wrong-creds path returns null directly with no per-user branching, the controller has a single static throw site for the 401 envelope. AuthModule ↔ TwoFaModule cycle resolved via `forwardRef(() => ...)` on BOTH sides. New api spec `apps/api/test/login-2fa-branch.spec.ts` (8 invariants); HEAD api suite now 32/32 GREEN. Web client: `LoginResponseSchema = z.union([stepUp, session])` (non-discriminated — 1FA branch has no `kind` field); `/login/page.tsx` checks `kind === "2fa-required"` and stashes `{token, twoFa}` in `sessionStorage` under `"sv:step-up"` for Plan 10's `/login/2fa` page (inter-wave gap acceptable — no test users have 2FA enrolled in any deployed environment yet). Wave 5 (Plan 03-09 — APP_GUARD ordering fix + email-key sha256 cap; closes FINDING-0021 + FINDING-0022) UNBLOCKED.

Findings disposition reminder (from `.planning/phases/03-2fa-sessions/03-INDEX.md` §"Forwarded findings disposition"): FINDING-0017 storage tier closed by Plan 03-01; FINDING-0021 + FINDING-0022 throttler-tier closures land in Plan 03-09 (APP_GUARD ordering + sha256(email).slice(0,16) keying); FINDING-0011 (`/invite/redeem` email echo) deferred to Phase 13 (Phase 03 doesn't touch invite flow). Other 0012..0029 (Low/Info) all remain OPEN as tracked tech debt for Phase 13. See `.planning/security/FINDINGS.md`.

SMTP requirement re-deferred to Phase 07 in Phase 02 INDEX rationale: v1 invite delivery is out-of-band (operator runs `pnpm cli invite create --email …`, hand-delivers code via Signal/in-person/own email client). Operator may override and pull SMTP forward by raising the question; otherwise it stays Phase 07. Argon2id operator-calibration tool ships as part of Plan 06 — operator MUST run it on the VPS before production cutover (documented in Plan 12's runbook update). 02-06 already shipped the calibrator + the env-var documentation in `.env.example`; the api side will read those vars in 02-08.

## Session continuity

- **Last session:** 2026-05-04 — Wave 7 (Plan 03-12 T1–T3) closed. **All 12 Phase-03 plans complete pending two operator checkpoints** (Plan 10 T4 UX visual + Plan 12 T4 runbook decision).
- **Stopped at:** Plan 03-12 T1+T2+T3 closed (`b74ab0e`, `acbdc42`). New `docs/operator/RUNBOOK.md` (day-2 ops gap filled); SECURITY-NOTES.md extended with Phase-03 invariants section the auditor reads at the gate; .env.example adds SESSION_EPOCH_CACHE_TTL; new test-helpers controller (EXPOSE_TEST_ROUTES-gated, mirrors VaultProbeModule pattern) with 3 routes (flip-shared-vault-stub, seed-totp-credential, mutate-webauthn-counter); 2 LIVE Cypress specs (sessions.cy.ts, 2fa-removal.cy.ts) covering Truths 9-11+13 + Phase-07 hand-off seam; 2 SKIPPED Cypress stubs (2fa-webauthn.cy.ts, 2fa-totp.cy.ts) with detailed live-iteration documentation; CI workflow extended with EXPOSE_TEST_ROUTES=1. API + web typecheck + build green; 32/32 vitest specs green. Cypress live verification owed to the next CI push.
- **Resume:** **Two operator checkpoints owed before `/gsd:verify-work 3`**:
  1. **Plan 10 T4** — open `/settings/security` in a logged-in dev session, screenshot the passkey-vs-TOTP CTAs, save under `.planning/phases/03-2fa-sessions/03-10-uxcheck.png`. Then mark T4 ✅.
  2. **Plan 12 T4** — read `docs/operator/RUNBOOK.md` § "Lost 2FA — user can't sign in", record decision (a-accepted-indefinitely vs b-Phase-11-revisit) on the no-recovery-phrase-bypass rule in `03-12-SUMMARY.md` § "Operator decision recorded here once made". Then mark T4 ✅.
  Once both are done, run `/gsd:verify-work 3` to execute the 4-auditor + threat-modeler gate against the full Phase-03 surface.

## Changelog

- **2026-05-04** — Phase 03 / Wave 7 / Plan 03-12 (E2E Cypress + operator docs — final phase plan) executed across two atomic commits + 1 pending operator checkpoint. **`b74ab0e` T3 docs**: `docs/operator/RUNBOOK.md` (NEW file — day-2 ops gap that the pre-existing CLI/DOKPLOY-DEPLOY/LOCAL-DEV/SECURITY-NOTES quartet didn't cover) covering Phase-03 env-var matrix (WEBAUTHN_RP_ID/RP_NAME/ORIGIN, STEP_UP_TOKEN_TTL, SESSION_EPOCH_CACHE_TTL, EXPOSE_TEST_ROUTES with prod values + load-bearing notes), lost-2FA recovery procedure (operator psql + redis-cli inside Dokploy api container — recovery phrase explicitly does NOT bypass 2FA per threat model), WebAuthn RP-ID change procedure (credential-bricking event + 7-day notice + bulk-delete + bulk session_epoch bump), EXPOSE_TEST_ROUTES production-safety verification (Dockerfile/compose grep + Dokploy panel inspection + container env grep — P0 incident if any prod hit), operator-initiated single-user/all-users session revocation procedures with the load-bearing "always pair the bump with a Redis cache DEL" note. `docs/operator/SECURITY-NOTES.md` extended with "Phase 03 invariants" section the auditor reads at the gate (TOTP secret browser-only + server-side grep verification, AAD label scheme `sv:<scope>:v1|`, session-epoch column + Redis cache + bust semantics, WebAuthn RP-ID load-bearing pointer to RUNBOOK, complete `@Public()` route allow-list, 2FA-required guard hand-off seam to Phase 07, 12 audit-action enum extensions FROZEN for Phase 10's hash-chain indexing, findings disposition table). `.env.example` adds `SESSION_EPOCH_CACHE_TTL=60`. **`acbdc42` T1+T2 specs/helpers/CI**: `apps/api/src/test-helpers/{test-helpers.controller.ts,test-helpers.module.ts}` — three EXPOSE_TEST_ROUTES-gated POST routes (`flip-shared-vault-stub {value: bool}`, `seed-totp-credential {email, name} → {id}`, `mutate-webauthn-counter {credentialId, counter}`) mirroring Plan 03-07's VaultProbeModule pattern; conditional registration in app.module.ts alongside VaultProbeModule. `apps/web/cypress/e2e/sessions.cy.ts` — covers Truths 11 + 13 (current session pinned + empty-other-sessions state, revoke-all wipes local + redirects); Truth 12 deferred to live-iteration (multi-session setup needs cy.session machinery or a new cy.task — would require introducing setupNodeEvents). `apps/web/cypress/e2e/2fa-removal.cy.ts` — covers Truth 10 + the Phase-07 hand-off seam end-to-end: signup → 1FA login → cy.request seed-totp-credential → cy.request flip-shared-vault-stub:true → visit /settings/security → click Remove → assert AUTH_2FA_REMOVAL_BLOCKED 409 surfaces in DOM as the forward-looking shared-vault copy → reset stub. **`apps/web/cypress/e2e/2fa-webauthn.cy.ts` and `2fa-totp.cy.ts` are DESCRIBE.SKIP'd** with detailed comment blocks documenting the live-iteration owed — WebAuthn needs CDP virtual-authenticator wiring in setupNodeEvents (Cypress 14 doesn't ship a stable wrapper; current config uses cy.exec not cy.task), TOTP needs operator decision between cy.stub `sodium.randombytes_buf` vs test-only `window.__SV_TEST_TOTP_SECRET__` seam when EXPOSE_TEST_ROUTES === "1". Status table in 03-12-SUMMARY makes the coverage gaps explicit for the auditor. `.github/workflows/ci.yml`: rename e2e job to mention Phase 03 specs + add `EXPOSE_TEST_ROUTES: "1"` to the api env block (CI runs `cypress:run` against the `cypress/e2e/**/*.cy.ts` glob — new specs auto-included). API typecheck + build + 32/32 vitest specs all green. Cypress live verification owed to the next CI push. **Decisions recorded** in 03-12-SUMMARY: two LIVE specs + two SKIPPED stubs (more honest than four flaky specs); multi-session E2E deferred (cy.session machinery owed); one test-helpers controller with multiple seams (vs three tiny modules); `@Public()` on test-helpers (production safety = the EXPOSE_TEST_ROUTES build flag, JWT auth would just complicate test setup); `seed-totp-credential` uses placeholder bytes (server doesn't decrypt for listing/removal flow — saves complexity by NOT producing a real wrap); CI gets EXPOSE_TEST_ROUTES=1 baked into the e2e job + production-safety story documented in RUNBOOK + SECURITY-NOTES; no setupNodeEvents block introduced (cy.exec pattern preserved); RUNBOOK.md is a NEW file filling the day-2-ops gap. **T4 (operator runbook review) PENDING** — operator owes a recorded decision on the no-recovery-phrase-bypass rule (a-accepted-indefinitely vs b-Phase-11-revisit). **Phase 03 12/12 plans complete; awaiting Plan 10 T4 UX visual + Plan 12 T4 runbook decision before `/gsd:verify-work 3`.** See `.planning/phases/03-2fa-sessions/03-12-SUMMARY.md`.

- **2026-05-04** — Phase 03 / Wave 6 (Plans 03-10 + 03-11 web 2FA + sessions UI) executed across five atomic commits + 1 pending operator checkpoint. **Plan 03-11** (web /settings/sessions): `7affc1e` T1 + `2bf83f4` T2. New `apps/web/src/lib/api/sessions-client.ts` (typed wrappers reusing the shared `request` helper exported from auth-client.ts in this commit so twofa-client + sessions-client + auth-client all share one fetch+Zod+error-envelope path). New `apps/web/src/app/(authed)/settings/sessions/{page,session-list,revoke-button,revoke-all-button}.tsx` — current row pinned + emerald-accent panel; `Intl.RelativeTimeFormat` for relative timestamps (avoids the date-fns ~40 KB dep); 404 on revoke-one collapses to generic "couldn't revoke" (preserves server-side anti-enumeration); revoke-all is confirm-then-execute with full local wipe (`keyStore.wipe() + accessTokenStore.wipe()`) + hard redirect to /login. Defence-in-depth wipe-on-API-failure mirrors the Phase-02 logout pattern. Cross-tab implication documented inline (revoke-all bumps users.session_epoch → other open tabs lose access within ≤60s SessionEpochCache TTL). **Plan 03-10** (web /settings/security 2FA enrol + /login/2fa step-up consumer): `e2e8905` T1 + `df46767` T2 + `e18821d` T3. T1 ships /settings/security skeleton + `<MethodList>` + UX copy; passkey panel emerald-bordered + "Recommended — phishing-resistant" badge as PRIMARY CTA; TOTP panel zinc + amber-warning panel ABOVE the enrol area carrying the verbatim THREAT-MODEL §17 copy "Authenticator-app codes can be entered into a phishing site. Use a passkey when possible — passkeys cryptographically refuse to log in to a fake site."; method list shows PASSKEY (emerald) vs AUTHENTICATOR APP (amber) badges; Remove button surfaces the Phase-07-aware AUTH_2FA_REMOVAL_BLOCKED 409 with forward-looking shared-vault copy. T2 wires `<EnrollPasskeyButton>` via `@simplewebauthn/browser@^11` (pinned to match `@simplewebauthn/server@^11` server-side); NotAllowedError → graceful "Cancelled"; bundle delta /settings/security 127 → 130 kB First-Load (+3 kB gzipped). **T3 has three sub-deliverables**: (a) NEW API endpoint `GET /2fa/step-up-material` (step-up guarded) returning `{userArgonSalt, argon2Params, wrappedMasterDek, totpCredentials[]}` — folds wrapped TOTP secrets + unwrap material in one roundtrip. **Rule-3 architectural deviation**: the plan called out only `GET /2fa/totp/credentials` but completing TOTP step-up client-side ALSO needs `userArgonSalt + argon2Params + wrappedMasterDek` since the 2FA-required login response per Plan 08 deliberately carries no wrapped material. Folding into one endpoint saves a roundtrip + keeps new surface to one route. Throttled at the existing `2fa-verify-ip` (30/min IP-keyed); `@Public() + @UseGuards(Require2FAStepUpGuard)`. (b) TOTP enrolment `apps/web/src/app/(authed)/settings/security/enroll-totp-flow.tsx` + `apps/web/src/lib/crypto/totp-wrap.ts` + `AAD_LABEL_TOTP = "sv:user-totp:v1|"` in `aad-labels.ts`. Flow: begin-register (server reserves issuance nonce TTL 120s) → 20-byte secret via libsodium → buildOtpauthUrl + qrcode-generated QR → user enters code → `verifyTotpCandidate` ±1 step locally → wrap secret under master_DEK with `encodeAad(argon2Params, AAD_LABEL_TOTP || SHA256(lower(email)))` → finish-register; best-effort secret zeroing on every transition. (c) `/login/2fa` step-up consumer `apps/web/src/app/login/2fa/page.tsx` + `apps/web/src/lib/auth/step-up-flow.ts` (completeWithPasskey + loadStepUpMaterial + findTotpMatch + completeWithTotp + applyStepUpSession primitives). Passkey path runs WebAuthn ceremony directly. TOTP path GETs step-up material → derives master_KEK locally from keyStore-preserved password+secret_key → unwraps master_DEK → walks ALL user TOTP credentials × ±1-step drift to identify which credential matches the typed code → POSTs verify. On success applyStepUpSession runs the same `unlockSecrets` path as 1FA login. On missing handoff/expired step-up wipes everything + bounces to /login. **/login/page.tsx Rule-3 deviation**: switched 2FA-required branch from `window.location.assign("/login/2fa")` to soft `router.push("/login/2fa")` so the JS realm survives — keyStore singleton preserves password+secret_key+email across navigation (stashed under `step_up_email/step_up_password/step_up_secret_key` keys). Hard nav would have torn down the keyStore before /login/2fa loaded; without keyStore-preserved typed credentials the user would have to re-enter password+secret_key at /login/2fa, terrible UX. 1FA-only branch unchanged byte-for-byte. **Server-side grep clean** for master_DEK / computeTotpStep / verifyTotpCandidate in apps/api/src/twofa/totp/ — server only round-trips opaque bytes (Plan 03 Key Link 3). Two new web deps: `@simplewebauthn/browser@^11`, `qrcode + @types/qrcode`. Bundle deltas: `/settings/security` 548 kB First-Load, `/login/2fa` 538 kB (libsodium dominates — already loaded by /login + /signup, Webpack shares the chunk). Pre-existing api lint count unchanged at HEAD. 32/32 vitest specs green. **T4 (operator UX checkpoint) PENDING** — code-side verification clean (grep shows passkey panel above TOTP panel with "Recommended — phishing-resistant" badge; amber warning panel above TOTP enrol button carrying the verbatim phishing copy on lines 74-75 of page.tsx); operator owes a screenshot at `.planning/phases/03-2fa-sessions/03-10-uxcheck.png` confirming visual rendering. **Decisions recorded** in 03-10-SUMMARY: ONE step-up-material endpoint instead of two; soft router.push (not window.location.assign); password/secret_key in keyStore (not sessionStorage — XSS attack surface minimisation); AAD bytes round-tripped through server (defence in depth); Intl.RelativeTimeFormat over date-fns; `@simplewebauthn/browser@^11` pin matches server; no auth-context.tsx extension (Plan 08 sessionStorage handoff already works end-to-end). No Rule 4 deviations beyond the architectural ones above. **Wave 7 (Plan 03-12 — Cypress 2fa-webauthn + 2fa-totp + sessions specs + runbook update for new env vars) UNBLOCKED.** See `.planning/phases/03-2fa-sessions/03-10-SUMMARY.md` and `03-11-SUMMARY.md`.

- **2026-05-04** — Phase 03 / Plan 09 (throttler ordering fix + login-email keying — FINDING-0021 + 0022 closure) executed across three atomic commits: `859c7e4` (T1) `apps/api/src/auth/jwt/public.decorator.ts` (NEW — `IS_PUBLIC_KEY` metadata key + `Public()` SetMetadata wrapper) + `apps/api/src/auth/jwt/jwt-auth.guard.ts` (Reflector dependency + `getAllAndOverride([handler, class])` short-circuits to `true` BEFORE bearer-token inspection) + `apps/api/test/jwt-epoch.spec.ts` (3-arg `JwtAuthGuard` ctor: `new Reflector()` injected; mock `ExecutionContext` exposes `getHandler` + `getClass` returning marker functions without `IS_PUBLIC_KEY` metadata so the auth path runs); `8e3215c` (T2) `apps/api/src/app.module.ts` (REGISTRATION ORDER LOAD-BEARING — `JwtAuthGuard` registered as APP_GUARD BEFORE `SimpleVaultThrottlerGuard`; NestJS APP_GUARD semantics: second guard fires only if first allowed, so `req.user.id` is populated when `generateKey` runs) + `@Public()` applied to 10 controller methods (`/health`, `/invite/redeem`, `/auth/{signup,login,refresh,logout,params}`, plus `/2fa/webauthn/{begin,finish}-auth` via class-level `@Public()` on `WebauthnAuthController` and `/2fa/totp/verify` via method-level `@Public()` — these step-up routes carry `purpose:"2fa-stepup"` tokens which `JwtAuthGuard` rejects via the Plan-02 pre-decode discriminator and authenticate via `Require2FAStepUpGuard` instead); pre-existing per-controller `@UseGuards(JwtAuthGuard)` on `/me`, `/2fa/{webauthn,totp}/{begin,finish}-register`, `/2fa/methods`, `/sessions/*`, `/vault/_2fa-guard-probe` kept as harmless defence-in-depth (second invocation = Reflector hit + SessionEpochCache Redis hit, ~1ms); `a4283a0` (T3) `apps/api/src/common/throttler.config.ts` (`generateKey` for `login-email` now keys via `createHash("sha256").update(email).digest("hex").slice(0,16)` — fixed 16-char tracker; bounds Redis key memory under arbitrary-length email floods + removes lowercased-email PII from Redis keys; `node:crypto` import; same email always hashes the same way so existing 10/email/h semantics preserved) + header comment block documenting load-bearing invariant: throttler runs BEFORE Zod validation pipes (guards before pipes), so any future ceiling keyed off `req.body` MUST hash + slice. **Truths 18 + 19 verified** at the code layer: APP_GUARD ordering verified by `grep -n APP_GUARD apps/api/src/app.module.ts` (JwtAuthGuard provider appears first); throttler key derivation verified in `generateKey`. **HEAD api test suite 32/32 GREEN** — no regressions; jwt-epoch.spec.ts updated for the 3-arg ctor. `pnpm typecheck` + `pnpm build` green; lint at HEAD has the same 8 pre-existing errors as parent commit `5049b0b` (4 in `methods.service.ts` userHasSharedVaultDependency stub + Number() no-op conversions; 4 parsing errors on test files outside the lint tsconfig project — NO new errors introduced). **Live e2e reproduction** (101 `/me` from IP-1 then one from IP-2 with the same JWT must 429; `redis-cli --scan --pattern 'login-email:em:*'` after a 4KB-email flood must return fixed 16-hex keys) **deferred to `/gsd:verify-work 3`** and Plan 12's Cypress run — the unit suite verifies the guard contract; the live test verifies the APP_GUARD framework wiring. **FINDING-0021 + FINDING-0022 → FIXED-PENDING-VERIFICATION** (both Medium; updated in `.planning/security/FINDINGS.md` with resolved-by-commits + verification-pending notes). **Decisions recorded** in 03-09-SUMMARY: class-level `@Public()` on WebauthnAuthController (both methods share step-up auth); method-level `@Public()` on TotpController.verify only (sibling /begin-register + /finish-register still need access JWT); kept all pre-existing `@UseGuards(JwtAuthGuard)` decorators (harmless defence-in-depth — Plan 09 calls this "optional cleanup"); hex-slice(0,16) chosen over base64url for ASCII consistency with other tracker prefixes; `node:crypto` per codebase convention. No Rule 4 deviations. **Wave 6 (Plans 10 + 11 web settings UI, parallel) UNBLOCKED.** See `.planning/phases/03-2fa-sessions/03-09-SUMMARY.md`.

- **2026-05-02** — Phase 03 / Plan 08 (`/auth/login` branches on 2FA presence + web client handles discriminated response) executed across two atomic commits: `73cef47` (T1) `apps/api/src/auth/login/login.service.ts` (returns `LoginResult = LoginOk | LoginStepUp` discriminated by `kind`; after constant-time compare passes calls `methods.countActive(user.id)` and when `>=1` signs a step-up token via `stepUpJwt.sign(user.id, currentEpoch)` — emits `auth.login.step_up_issued` with `data.kind:"step-up"`; when 0 falls through to Phase-02 session-mint code unchanged with `data.kind:"session"` added to the existing `auth.login.ok` event for forensic partition) + `apps/api/src/auth/login/login.controller.ts` (branches on `result.kind`; 2fa-required path SKIPS the `__Host-refresh` cookie — user has no refresh family yet, family is created by `/2fa/.../finish-auth` or `/2fa/totp/verify`) + `apps/api/src/auth/login/login.dto.ts` (new `LoginStepUpResponseBody`) + `apps/api/src/auth/auth.module.ts` + `apps/api/src/twofa/twofa.module.ts` (`forwardRef(() => ...)` on BOTH sides — resolves the AuthModule ↔ TwoFaModule cycle introduced when LoginService started consuming MethodsService + StepUpJwtService) + `apps/api/src/twofa/methods/methods.service.ts` (new `countByKind(userId): {webauthn, totp}` primitive — `countActive` is now derived as the sum, single source of truth) + `apps/api/src/common/audit-events.ts` (`AuditAction.LoginStepUpIssued = "auth.login.step_up_issued"`) + `packages/shared/src/zod/index.ts` (`LoginStepUpResponseSchema`) + `apps/api/test/login-2fa-branch.spec.ts` (8 invariants — wrong-creds across 2FA-free + 2FA-enrolled users both return null byte-equal; correct creds + 0 methods → `kind:"session"` Phase-02 body intact + refresh issued; correct creds + ≥1 method → `kind:"2fa-required"` with stepUpToken + twoFa flags + NO accessToken + NO refresh; step-up token claims `purpose:"2fa-stepup"` + epoch matches user's session_epoch + exp = iat + 120; step-up token rejected by `JwtService.verifyAccessToken` per Key Link 5; per-kind availability flags reflect counts); `713a5fb` (T2) `apps/web/src/lib/api/auth-client.ts` (split `LoginResponseSchema` into `LoginSessionResponseSchema` (Phase-02 body unchanged, no `kind` field — regression-free) + `LoginStepUpResponseSchema`; `LoginResponseSchema = z.union([stepUp, session])` — non-discriminated because Phase-02 branch has no `kind` field; new types `LoginSessionResponse` + `LoginStepUpResponse` + the union `LoginResponse`) + `apps/web/src/app/login/page.tsx` (after `apiLogin()` resolves, `(loginRes as { kind?: unknown }).kind === "2fa-required"` → stash `{token, twoFa}` in sessionStorage under `"sv:step-up"` + wipe partial 1FA in-memory state + redirect to `/login/2fa` (404 until Plan 10 — inter-wave gap acceptable: no test users have 2FA enrolled in any deployed environment yet); 1FA-only branch unchanged Phase-02 flow). **Truth 8 verified.** Anti-enumeration intact: `LoginService` runs the constant-time compare BEFORE any 2FA-table read; wrong-creds path returns `null` directly with no per-user branching; the controller's wrong-creds throw is a single static envelope. All 32/32 vitest specs green (8 new + 24 prior across `jwt-epoch.spec.ts`, `2fa-removal.spec.ts`, `2fa-required-guard.spec.ts`). `pnpm --filter @simplevault/{api,shared,web} build` all green. Three small auto-deviations (Rules 1-3 only): (a) Rule 3 blocking — AuthModule ↔ TwoFaModule cycle resolved via forwardRef on BOTH sides (NestJS standard idiom; alternative submodule extraction would have churned 6 files for 4 lines of behavioural change); (b) Rule 1 — TS doesn't narrow the union via `if ("kind" in loginRes)` because the 1FA branch has no `kind` field at all; used `Exclude<typeof loginRes, { kind: "2fa-required" }>` for the post-early-return narrow; (c) Rule 2 defence-in-depth — dual-emit pattern (`auth.login.ok` with `data.kind:"session"` + `auth.login.step_up_issued` with `data.kind:"step-up"`) gives operator dashboard two orthogonal forensic axes (action name AND data.kind) per Truth 8 must-have. **Decision recorded**: NO `kind` field on the 1FA-only branch (the plan's pseudocode showed both branches discriminated, but the must-have explicitly demands "Phase-02 byte-equal regression-free"). Web client distinguishes via `"kind" in body`. **Inter-wave gap**: Plan 10 ships `/login/2fa` page consuming the sessionStorage hand-off; until then a 2FA-enrolled web user lands on a 404 (acceptable — no deployed user has 2FA enrolled). No Rule 4 deviations, no CHECKPOINT. **Wave 5 (Plan 03-09 — throttler ordering fix) UNBLOCKED.** Hand-offs documented in `03-08-SUMMARY.md`: Plan 09 must keep `/auth/login` on the `@Public()` allow-list; Plan 10 reads `sessionStorage.getItem("sv:step-up")` for the 2FA ceremony hand-off; Plan 12 should add a Cypress byte-equality assertion of the 401 envelope across 2FA-enrolled vs 2FA-free wrong-creds paths. See `.planning/phases/03-2fa-sessions/03-08-SUMMARY.md`.

- **2026-05-02** — Phase 03 / Plan 04 (session-epoch JWT claim + JwtAuthGuard epoch check) executed TDD across three atomic commits: `4adfc6f` (T1 RED) `apps/api/test/jwt-epoch.spec.ts` + `apps/api/vitest.config.ts` + `apps/api/package.json` (vitest devdep + `test` script `vitest run`); `aae6636` (T2 GREEN) `apps/api/src/auth/sessions/session-epoch.cache.ts` (NEW — Redis-backed per-user epoch cache, TTL 60s via `SESSION_EPOCH_CACHE_TTL`, in-flight Map coalesces concurrent cold-cache reads, bustGen counter closes the writer-DEL-then-reader-SET race) + `apps/api/src/auth/jwt/jwt.service.ts` (`AccessTokenClaims.epoch: number` + sign + verify enforcement) + `apps/api/src/auth/jwt/jwt-auth.guard.ts` (DI `SessionEpochCache` + epoch-mismatch 401 `AUTH_SESSION_REVOKED`) + `apps/api/src/auth/auth.module.ts` (register/export cache) + `packages/shared/src/error-codes.ts` (`AUTH_SESSION_REVOKED: "E1017"`); `b3bc306` (T3 wire-up) `apps/api/src/auth/sessions/session.service.ts` (DI cache + `getEpoch`/`bumpEpoch` helpers; UPDATE-then-DEL ordering documented in jsdoc) + `apps/api/src/auth/login/login.service.ts` + `apps/api/src/auth/refresh/refresh.controller.ts` (replace `epoch:0` stubs Plan 02 left for hand-off with `await this.sessions.getEpoch(userId)`). **8 invariants RED → GREEN** in vitest: epoch claim signed, guard 401s on stale epoch, fresh tokens accepted, cache hit avoids DB, concurrent cold reads coalesce to 1 DB call, bumpEpoch racing concurrent reads ends with new epoch in cache, user-A bump leaves user-B cache untouched, guard hits cache once. **Cache-busting strategy DEL-after-UPDATE (NOT SET-with-new-value)** — class-level jsdoc on `SessionEpochCache` walks the race timeline; `bustGen` generation counter closes the second race (writer DEL during reader DB hop) by gating the inflight handler's post-read SET on "did anyone bust during my read?". **Worst-case revocation latency** = `min(SESSION_EPOCH_CACHE_TTL, ACCESS_TOKEN_TTL, time-since-bumpEpoch-cache-bust)` ≈ next-request RTT under healthy Redis. **Closes Phase-02 deferred REQ-AUTH-004** (instant access-token revocation) + **AT-5 leaf A residual** → MITIGATED-WITHIN-EPOCH-LATENCY. **Per-session DELETE (`DELETE /sessions/:id`) intentionally does NOT call bumpEpoch** — Plan 05's revoke-all is the sole producer (Key Link 3: bumping per single-session-revoke would invalidate sibling sessions' access tokens). **First vitest target inside `apps/api`** — mock-based (fake Redis Map + DbStub with snapshot-then-sleep semantics so race invariants are observable); real PG + Redis e2e validation in Plan 12 CI. Three small auto-deviations (Rules 1/2/3): (a) Rule 3 blocking — initial T1 commit folded my files into Plan 02's `645c00f` via an accidental `--amend`; recovered via `git reset --soft + cherry-pick 645c00f` (Plan 02 now lives at `5ae823d` with identical diff); (b) Rule 1 — DB stub initially read `rows.get(userId)` AFTER the artificial sleep so the bumpEpoch-racing-reads test passed trivially; sharpened to snapshot-at-start-then-sleep so the read-then-bump-then-set worst case is actually exercised; (c) Rule 2 defence-in-depth — added `bustGen` generation counter to `SessionEpochCache` (the plan's plain `inflight + redis.set` proposal leaves a stale-set window the counter closes). Plan-listed twofa stub-replacement sites (`apps/api/src/twofa/{webauthn/webauthn-auth.service,totp/totp.service}.ts`) are owned by Plans 02 + 03 parallel work (not yet committed); per "touch only files in your block" they're left for those plans' close-out hand-off. Build gate `pnpm --filter @simplevault/api build` is RED for files outside this plan (Plan 02's `@simplewebauthn/server` v11 type drift in `twofa/webauthn/*` + Plan 03's `sql` import missing in `twofa/totp/*`); MY files all typecheck clean. After Plans 02 + 03 close, the package build will go GREEN. No Rule 4 deviations, no CHECKPOINT. **Plan 03-05 (sessions endpoints) UNBLOCKED** — `bumpEpoch` is the primitive `revoke-all` will call; `DELETE /sessions/:id` only touches refresh family (`revokeFamilyByToken` parameterised by sessionId). Hand-offs documented in `03-04-SUMMARY.md`. See `.planning/phases/03-2fa-sessions/03-04-SUMMARY.md`.

- **2026-05-02** — Phase 03 / Plan 01 (2FA + sessions DB schema foundation) executed across three atomic commits: `bf642c5` (T1) `packages/db/src/schema/{webauthn_credentials,webauthn_challenges,totp_credentials}.ts` + barrel + `drizzle.config.ts` schema-list extension; `33b97ba` (T2) `packages/db/src/schema/users.ts` (`sessionEpoch: integer("session_epoch").notNull().default(0)` between `recoveryHmac` and `userPubKey`) + generated `drizzle/0002_phase03_2fa_sessions.sql` (renamed from `0002_silent_umar.sql`) + `meta/0002_snapshot.json` + `meta/_journal.json` tag rename; `6549e9a` (T3) `packages/db/src/schema/{users,invite_codes}.ts` (`text → varchar(254)`) + extended migration with `ALTER COLUMN ... USING substring(email,1,254)` (with prepended PL/pgSQL DO-block pre-flight guard that raises if any pre-existing row has email > 254) + `apps/api/src/auth/login/login.dto.ts` (`.max(254)` on `LoginSchema.email`) + `packages/shared/src/zod/index.ts` (`.max(254)` on `MeResponseSchema.email` — Rule-2 defence-in-depth) + `apps/cli/src/commands/invite-create.ts` (254-char ceiling guard with `process.exit(2)` fail-fast). Three new tables: `webauthn_credentials` (one row per passkey: `credential_id` UNIQUE for passkey discovery, `counter` bigint default 0 for clone detection, `transports` text[], `aaguid` bytea? for vendor identification, `name` user-chosen, `last_used_at?`); `webauthn_challenges` (short-lived ceremony nonces: `kind` text "register"|"auth", UNIQUE `(user_id, kind)` enforces "one in-flight per kind", `expires_at` index for opportunistic sweep — TTL enforced at app layer via atomic `DELETE … RETURNING`, no TOCTOU); `totp_credentials` (server-opaque `wrapped_secret` + `encrypted_secret_aad`, `last_used_step` bigint default 0 for atomic-CAS replay guard `UPDATE … WHERE last_used_step < $cs RETURNING *`). All three FK on `users.id ON DELETE CASCADE`. **Single migration** `0002_phase03_2fa_sessions.sql` carries the three table creates, the `session_epoch` column add, AND the email type tightening — merged by hand from a Drizzle-Kit-emitted incremental `0003_*.sql` per plan ("the diff appends … to the same migration"); chain repointed (`prevId` of 0002 snapshot = 0001 snapshot id), `db:generate` re-run confirms `No schema changes` (snapshot integrity verified). Migration runtime apply on `postgres:18.3-alpine` deferred to Plan 12 e2e CI job (no live PG this session); the migration is structurally valid + the `USING substring(...)` cast is idempotent for our cardinality (Phase 02 close: ≤50 users). **Findings disposition**: FINDING-0017 storage tier CLOSED here (varchar(254) on both email columns + Zod `.max(254)`); FINDING-0022 partially mitigated (Redis-key length now bounded by storage), full closure in Plan 03-09 via `tracker = "em:" + sha256(email).slice(0,16)` keying. Plan-listed but n/a sites: `SignupSchema.email` and `InviteRedeemSchema.email` — neither schema has an email field (server takes email from the locked invite row inside the signup tx; redeem body is `{ code }` only). Four small auto-deviations (Rules 1/2/3): (a) Drizzle Kit emits a fresh `0003_*.sql` instead of extending `0002` so manual merge + chain repoint required (Rule 3 blocking); (b) Drizzle Kit's random-word migration tag `0002_silent_umar` renamed to plan-spec `0002_phase03_2fa_sessions` (Rule 1); (c) unused `text` import dropped from `users.ts` and `invite_codes.ts` after switching email to `varchar` (Rule 1, TS6133); (d) `MeResponseSchema.email` capped at `.max(254)` as Rule-2 defence-in-depth (server `.parse()`s its own `/me` output). No Rule 4 deviations, no CHECKPOINT. **Wave 2 (parallel: Plans 02 WebAuthn API / 03 TOTP API / 04 session-epoch JWT claim) UNBLOCKED.** Hand-offs documented in `03-01-SUMMARY.md` §"Hand-offs to Wave 2": atomic challenge consume pattern (`DELETE … WHERE id = $1 AND expires_at > now() RETURNING challenge`); TOTP replay-guard pattern (`UPDATE … WHERE last_used_step < $cs RETURNING id`); session-epoch JWT-claim wiring (cache TTL 60s, busted on every column write); Plan 09 closing FINDING-0022 fully via the sha256 keying. See `.planning/phases/03-2fa-sessions/03-01-SUMMARY.md`.

- **2026-05-02** — Phase 02 (Auth + Crypto core) **CLOSED**. `/gsd:verify-work 2` ran the full gate: 1 goal-backward verifier (`gsd-verifier` 12/12 truths PASS, written to `.planning/phases/02-auth-crypto/02-VERIFICATION.md`) + 5 blocking auditors in parallel + 1 informational threat-modeler. Reports under `.planning/security/audit-reports/2026-05-02-{crypto,auth-flow,owasp-top10,input-validation,rate-limit-dos,threat-modeler}-auditor-phase02.md`. Initial verdict was BLOCKED by **FINDING-0010** (HIGH, auth-flow-auditor): `apps/api/src/common/filters/all-exceptions.filter.ts:14-16,58` embedded per-request `requestId` in the JSON error body, breaking byte-equal failure-response invariant on `/auth/login` + `/invite/redeem`; the Cypress sad-path spec at `apps/web/cypress/e2e/auth-sad.cy.ts:64,133` does `JSON.stringify(body)` byte-equality and would have failed in CI under nestjs-pino's per-request id auto-population. **Fix applied**: moved `requestId` from JSON body to `X-Request-Id` response header (`all-exceptions.filter.ts:58` `res.setHeader("X-Request-Id", requestId)`); body is now exactly `{error:{code,message}}`. Web `apps/web/src/lib/api/auth-client.ts` updated — `ErrorEnvelopeSchema` no longer accepts `requestId`, both `request()` and `postJson()` helpers read `x-request-id` from `res.headers` (case-insensitive `Headers.get`), `AuthClientError.requestId` still surfaced for client-side debugging. CORS hardening in `apps/api/src/main.ts:48` adds `exposedHeaders: ["X-Request-Id"]` so cross-origin web (dev-only; prod is same-origin via Traefik path-routing under `pass.runadev.com`) can read it. Inbound `X-Request-Id` is NOT trusted/echoed (pino-http's default counter-based `genReqId` is the sole source; verified). Typecheck + build green on api + web. **Re-run of `auth-flow-auditor`** appended `## RE-RUN 2026-05-02 — FINDING-0010 closure verification` to its report; FINDING-0010 flipped `OPEN → VERIFIED-CLOSED 2026-05-02` in `.planning/security/FINDINGS.md`. Final gate verdict: PASS. Other findings tracked OPEN as Medium/Low/Info (none block phase merge): FINDING-0011..0029. Most architecturally interesting four for Phase 03 fix-or-defer decision: **FINDING-0021** (`apps/api/src/common/throttler.config.ts:75-89` + `app.module.ts:143` — global `APP_GUARD` `SimpleVaultThrottlerGuard` runs BEFORE `JwtAuthGuard`, so `req.user` is unset when `generateKey` evaluates the `me-user` branch → REQ-RATELIMIT-006 user-keyed `/me` 100/min silently falls back to IP-keying, penalising shared NATs and missing the per-user invariant), **FINDING-0022** (`apps/api/src/common/throttler.config.ts:83-87` reads `req.body.email` pre-Zod with no length cap → can flood Redis with arbitrary-length unique keys; cross-references FINDING-0017 unbounded `users.email`/`invite_codes.email` columns + `LoginSchema.email` with no `.max(254)`), **FINDING-0011** (`apps/api/src/invite/invite.service.ts:64-69` — `/invite/redeem` echoes the invite-bound `email` on success, leaking PII to anyone holding a leaked code), **FINDING-0017** (`packages/db/src/schema/users.ts:35` + `invite_codes.ts:35` + `apps/api/src/auth/login/login.dto.ts:23` — defence-in-depth gap, no length cap on email at storage or DTO tier). Threat-modeler appended `THREAT-MODEL.md` §14–§18: §14 Phase-02 STRIDE per data flow (7 tables), §15 AT-5 reframed as "Adversary obtains a valid user session" (7 leaves: stolen access token MITIGATED, stolen refresh token MITIGATED, credential stuffing MITIGATED, invite hijack PARTIAL, client-side malware RESIDUAL, phishing RESIDUAL, backend compromise RESIDUAL for session-forge / MITIGATED for vault-read via E2E), §16 five new load-bearing assertions (pre-auth global `serverArgonSalt` disclosure ACCEPTED, `__Host-refresh` + same-origin under `pass.runadev.com` MUST hold in prod, AAD per-user binder = SHA256(lower(email)), timing-floor must remain constant-time-Argon2id NOT setTimeout, family-revoke transactional semantics), §17 priority shifts (M0 #5 KDF-downgrade + #8 account-enum flipped MITIGATED; new entries: phishing-without-WebAuthn HIGH for Phase 03, invite hijack via OOB MEDIUM for Phase 07, same-origin invariant breakage LOW now / HIGH if changed), §18 per-auditor follow-up pointers. Phase 02 status: **CLOSED**. Next action: `/gsd:plan-phase 3`.

- **2026-04-28** — Phase 02 / Plan 10 (web /signup multi-step flow) executed across three atomic commits: `7166e89` (T1) state machine + invite/master-pw/secret-key steps + `auth-client.ts` (Zod-validated fetch) + in-memory `key-store.ts` + `@simplevault/crypto` Ed25519 backfill (`sign.ts` + browser/node barrels + parity-test snapshot bumped); `c2b761a` (T2) recovery-phrase reveal (24-word BIP-39 grid) + 4-position challenge confirm (libsodium `randombytes_uniform` for index pick; re-roll on wrong answer); `ae40648` (T3) Submitting step with full derive→wrap→POST→wipe pipeline (`buildSignupEnvelope` builds the 10-field body matching 02-07 byte-for-byte). **AAD per-user binder = `SHA256(lower(email))`** (LOAD-BEARING for 02-11 login unwrap; documented rationale in `02-10-SUMMARY.md` §2 — user_id unknown at signup, `emailHash` is stable + leaks no plaintext into AAD bytes + self-derivable at login). **Per-blob AAD label prefixes FROZEN**: `"sv:user-master:v1|"` (master_dek), `"sv:user-recovery:v1|"` (master_dek_recovery), `"sv:user-sign-sk:v1|"` (signing_sk), `"sv:user-kx-sk:v1|"` (kx_sk) — matches 02-04 hand-off doc. NO auto-login on signup → redirects to `/login?signed_up=1`. Secret lifecycle: master_password / secret_key / mnemonic / derived KEK+DEK + private keypairs live in component state + a singleton in-memory `keyStore` Map; on success `keyStore.wipe()` zeros every Uint8Array + explicit `.fill(0)` on locals + hard `window.location.assign("/login?signed_up=1")` for full-page reset. NEVER touches localStorage/sessionStorage/IndexedDB/cookies (verifiable in DevTools). CSP additions: `'wasm-unsafe-eval'` for libsodium WASM compilation (does NOT relax JS eval), `connect-src` widened to `NEXT_PUBLIC_API_URL` origin (prod via reverse-proxy = same-origin no-op). `/signup` chunk = 431 kB (libsodium WASM dominant); `node:crypto` regex clean across every `.next/static/chunks/**/*.js` (verified). Web app test infra deferred to 02-12 (no Vitest/RTL set up; reducer + helpers like `bytesToCrockford` and `pickIndices` exported testably for 02-12 spec). Manual smoke deferred to 02-12. **Crypto package: `generateSigningKeyPair` (Ed25519, 32 B pub + 64 B sk) added** as `packages/crypto/src/sign.ts`, exposed in BOTH browser and node barrels (no secret/password input → no server-invariant violation), parity test updated; `+./browser` exports subpath added so TS resolves runtime values cleanly without going through the type-only `index.d.ts`. Total crypto suite is now 71 tests (was 69, +2 sign.test.ts). Five rule-1/2 deviations under auto-apply: Ed25519 backfill scope creep (deferred from 02-04, the only consumer is 02-10), `'wasm-unsafe-eval'` CSP relaxation (necessary for WASM), `connect-src` widening (necessary for cross-origin API), `/browser` exports subpath (build-config), AAD context_id divergence from `user_id_bytes` doc-stated to `sha256(lower(email))` (rationale documented). No rule-4 deviations. **Plan 02-11 (web login + auto-refresh) UNBLOCKED** with hand-off: re-use `auth-client.ts` (extend with `login/refresh/logout/me`), re-use `key-store.ts`, byte-identical AAD label scheme; recommend introducing a shared `apps/web/src/lib/crypto/aad-labels.ts` constants module to avoid drift between signup-derivations and the login-derivations (which 02-11 will write). **Plan 02-12 (E2E + runbook) hand-off**: Playwright happy path = invite-via-CLI → 6-step wizard → assert `users` row populated + `invite_codes.redeemed_at` set + redirect to `/login?signed_up=1`; runbook update should mention `NEXT_PUBLIC_API_URL` web env + corresponding CSP `connect-src` knock-on. See `.planning/phases/02-auth-crypto/02-10-SUMMARY.md`.
- **2026-04-30** — Phase 02 / Plan 09 (GET /me + JwtAuthGuard + audit-events module + @nestjs/throttler + Pino redaction extension) executed across two atomic commits: `9779d46` (T1, prior agent) `apps/api/src/me/*` + `apps/api/src/auth/jwt/jwt-auth.guard.ts` + `packages/shared/src/zod/index.ts MeResponseSchema`; `510c6d0` (T2, this run) `apps/api/src/common/{audit-events.ts,throttler.config.ts}` (new) + `apps/api/src/common/rate-limit.ts` (deleted) + `app.module.ts` (extended Pino redaction + ThrottlerConfigModule + APP_GUARD wiring) + 5 controllers + 4 services refactored to call `AuditEventService.emit(...)` + `.env.example` (8 new env vars) + `apps/api/package.json` (`@nestjs/throttler@^6.5.0` + `@nest-lab/throttler-storage-redis@^1.2.0`). **`/me` 200 body shape LOCKED** at `MeResponseSchema = z.object({id, email, createdAt, argon2Params:{memoryKiB,iterations,parallelism:1}}).strict()` — `MeService.get` calls `.parse(out)` so any forbidden field that ever slips in throws and surfaces as `SERVER_INTERNAL` (defence in depth against schema-evolution leaks). **`AuditEvent` v1 contract FROZEN for Phase 10**: `{v:1, ts:ISO-8601, action, actorUserId, targetId, outcome:"ok"|"fail", reason?, ipHashB64?, uaFamily?, requestId?, data?:Record}` with 11-action enum (`auth.signup.{ok,fail}`, `auth.login.{ok,fail}`, `auth.logout`, `auth.refresh.{ok,fail,reuse_detected}`, `invite.redeem.{ok,fail}`, plus advisory `rate_limit.exceeded`). `AuditEventService.emit()` is the SINGLE funnel — escalates `outcome:"fail"` and `RefreshReuseDetected` to `warn`; Phase 10 swaps for an append-only hash-chain writer behind the same interface. All Phase 02 ad-hoc `evt:"auth.*"` Pino emissions refactored to call it; controller-vs-service double-logging on failure paths de-duplicated (controller owns ip/ua context). **`@nestjs/throttler` v6.5 over Redis** (lazy-connect ioredis + `nest-lab/throttler-storage-redis`) replaces the deleted in-memory `FixedWindowRateLimiter`; counters now share across replicas. Nine named ceilings env-tunable for 02-12: `default` 1000/IP/15min (REQ-RATELIMIT-001), `signup-ip` 3/IP/h (RATELIMIT-003), `login-ip` 5/IP/min (RATELIMIT-002), `login-email` 10/email/h (RATELIMIT-002, body-keyed), `refresh-ip` 60/IP/min, `logout-ip` 60/IP/min, `auth-params-ip` 100/IP/min, `invite-redeem-ip` 30/IP/h, `me-user` 100/user/min (RATELIMIT-006, post-JWT user-keyed). `SimpleVaultThrottlerGuard` (custom `APP_GUARD` subclass): `generateKey` keys `me-user` by `req.user.id`, `login-email` by lowercased `req.body.email`, others fall through to IP via `getTracker`; `canActivate` catches Redis storage errors (`ECONNREFUSED`/`ENOTFOUND`/`Stream isn't writeable`/`Connection is closed`) and **fails OPEN** with a warn-log (Phase 13 may tighten); `throwThrottlingException` sets `Retry-After: <ttl-seconds>` header on every 429; `skipIf` excludes `/health` (Dokploy probes). **Pino redaction list extended comprehensively** in `app.module.ts` — load-bearing reference for `auth-flow-auditor` + `owasp-top10-auditor`: HTTP headers (`authorization`/`Authorization`/`cookie`/`Cookie`/`set-cookie`/`Set-Cookie` on req+res), generic credentials (`password`/`secretKey`/`secret_key`/`recoveryPhrase`/`recovery_phrase`/`mnemonic`/`recovery`/`recovery_lookup_hash`/`recoveryLookupHash`/`jwt`/`totpCode`/`token`/`code`), every 02-07 signup bytea body field (snake + camel) (`argon2_secret_key_hash`, all four `wrapped_*` fields, `user_pub_key`, `recovery_inner_hash`, `user_argon_salt`, `server_argon_salt`), 02-08 access/refresh tokens, response bodies that emit wrapped material on /auth/login + /auth/refresh, AEAD body-level `nonce`, plus wildcard `*.<field>` paths for arbitrary nesting depth (`*.dek`, `*.kek`, `*.master_kek`/`masterKek`, `*.recovery_kek`/`recoveryKek`, `*.argon2*`, `*.wrapped*`, `*.userPubKey`/`user_pub_key`, `*.serverArgonSalt`/`userArgonSalt`, `*.recoveryHmac`/`recovery_hmac`, `*.recoveryPhrase`/`recovery_phrase`, `*.mnemonic`, `*.refreshToken`/`refresh_token`, `*.accessToken`/`access_token`, `*.password`, `*.secretKey`/`secret_key`, `*.recovery_lookup_hash`). **CSP `x-nonce` header INTENTIONALLY NOT redacted** (per-request public values; redacting would just hide useful debug info). **`.env.example` documents 8 new vars with safe defaults**: `ACCESS_TOKEN_TTL=900` + `REFRESH_TOKEN_TTL=2592000` (REQ-AUTH-001/002), `SERVER_IP_HASH_SECRET` (recommended ≥16 chars; warn-fallback to unkeyed SHA-256 in dev), `SERVER_ARGON_SALT` (REQUIRED prod, 16B base64), plus all nine rate-limit ceilings (`GLOBAL_RATE_LIMIT`, `LOGIN_IP_RATE_LIMIT`, `LOGIN_EMAIL_RATE_LIMIT`, `SIGNUP_RATE_LIMIT`, `REFRESH_IP_RATE_LIMIT`, `LOGOUT_IP_RATE_LIMIT`, `AUTH_PARAMS_RATE_LIMIT`, `INVITE_REDEEM_RATE_LIMIT`, `ME_RATE_LIMIT`). Verification: `pnpm --filter @simplevault/api typecheck/lint/build` all green; `pnpm audit --audit-level=high` clean; `grep -E 'logger\.(info\|warn\|log)' apps/api/src` shows only legitimate infra/lifecycle lines (DB pool init, Redis init, JWT init, throttler-storage-outage warn, IP-hash-secret fallback warn) — ZERO ad-hoc audit-style emissions; `grep -E 'FixedWindowRateLimiter\|clientIpKey\|rate-limit\.js' apps/api/src` zero hits (clean removal). Five small deviations under rules 1-2: (a) **no unit tests added** — the API package has no test infra (`"test": "echo 'no tests…' && exit 0"`), 02-07 + 02-08 also shipped without unit tests, the plan's `<verify>` says "manual or scripted", and full E2E is Plan 02-12's scope; (b) extra advisory action `rate_limit.exceeded` in v1 enum so Phase 13 can wire 429-into-audit without bumping `v`; (c) tighter per-minute throttler windows for IP-keyed ceilings (`login-ip` 5/min vs plan's 5/15min, `refresh-ip` 60/min, plus added `logout-ip` + `auth-params-ip` not enumerated) — same hourly throughput for legit clients, costlier for brute-force; `login-email` kept at 10/h (load-bearing anti-credential-stuffing); (d) Pino redaction adds snake_case + camelCase variants throughout since Drizzle raw `sql` returns snake_case; (e) Redis storage failure fails OPEN with warn-log (plan unspecified). No rule-3 deviations, no rule-4 (CHECKPOINT). **Plan 02-10 (web signup) UNBLOCKED**. **Plan 02-11 (web login) UNBLOCKED** with hand-off: `/me` returns `argon2Params` so client can detect operator-side calibration drift and re-derive `master_KEK` without full re-login (compare cached vs `/me.argon2Params` on app boot). **Plan 02-12 (E2E + runbook) hand-off**: assert `/me` returns exactly four keys; uniform 401 on no/expired/garbage token; 6th `/auth/login` burst is 429 with `Retry-After`; `/health` exempt from throttling; suggest setting `LOGIN_IP_RATE_LIMIT=1` for the burst test then resetting to defaults. **Phase 10 (audit hash-chain) hand-off**: drop-in replacement of `AuditEventService.emit` that computes `H_n = HMAC(SERVER_CHAIN_SECRET, H_{n-1} || canonical_json(event))` and appends `(seq, event, h_n)` to `audit_log` inside the same DB tx as the operation; canonical JSON / sorted keys for the free-form `data` field is critical to avoid chain breaks across producers. See `.planning/phases/02-auth-crypto/02-09-SUMMARY.md`.

- **2026-04-29** — Phase 02 / Plan 08 (POST /auth/login + /auth/refresh + /auth/logout) executed. Three atomic commits: `8a370d6` (T1) `apps/api/src/auth/{jwt,login,sessions}/*` + `apps/api/src/common/timing-floor.ts` + `auth.module.ts` wiring + extended Pino redaction + `jose@5` dep; `37e9bbe` (T2) `apps/api/src/auth/refresh/refresh.controller.ts`; `34d7252` (T3) `apps/api/src/auth/logout/logout.controller.ts`. **Library choice: jose** (HS256 + explicit `kid: "primary"` header, single-dep, Edge-compatible). **JWT claim shape**: `{sub: userId, sid: sessionId, fam: familyId, iat, exp}`; `kid` lives in protected header. **/auth/params (GET, public)** publishes the GLOBAL `argon2Params` so clients compute the verifier WITHOUT first leaking which emails exist (operator's anti-enumeration decision). **Login** lookups by `lower(email)` (functional UNIQUE index from 02-05); ALWAYS runs `crypto.timingSafeEqual` against the per-row `argon2_secret_key_hash` OR `DUMMY_HASH` (deterministic 32-byte SHA-256 over a versioned label + `JWT_SECRET` so the dummy isn't predictable); user-not-found and bad-verifier paths return 401 E1001 with byte-identical body shape. **Per-user `server_argon_salt`** (load-bearing carry-over) flows from the user row into the 200 response — never leaks on failure. **Cookie + body**: `accessToken` + wrapped material in JSON body, `Set-Cookie: __Host-refresh=<base64url>; HttpOnly; Secure; SameSite=Strict; Path=/; Max-Age=2592000` (REFRESH_TOKEN_TTL default 30d). **`__Host-` prefix** invariants (`Path=/` + `Secure` + no `Domain`) verified via curl. **Refresh rotation** is a single Drizzle `db.transaction` with `SELECT … FOR UPDATE`: row-not-found → 401 E1001; `used_at IS NOT NULL` → REUSE DETECTED → `UPDATE user_sessions SET revoked_at = now() WHERE family_id = $f AND revoked_at IS NULL` (load-bearing: must be inside the same tx so a parallel rotation can't slip a fresh row past the revoke) + emit `auth.refresh.reuse_detected` + 401 E1005; `revoked_at IS NOT NULL` OR `expires_at <= now()` → 401 E1001 (passive expiry NOT family-revoked); else INSERT new row (same `family_id`, `prev_token_id = old.id`, `expires_at = now() + REFRESH_TOKEN_TTL`) + UPDATE old row `used_at + revoked_at = now()`. **Refresh-token storage**: BLAKE2b-256 of base64url(32 random bytes) — fast integrity hash, raw never persisted. **Logout** family-revokes via `revokeFamilyByToken`; idempotent (no cookie → still 200, clears cookie); always sets `Max-Age=0` cookie. **Access JWT revocation deferred** to Phase 03 session-epoch (REQ-AUTH-004) — accepted trade-off, documented. **Logout-all-sessions deferred** to Phase 03. **E2E harness 21/21 PASS** against fresh `postgres:18.3-alpine`: happy login, wrong-creds-401, unknown-email-401 (uniform), timing-floor wrong-vs-unknown delta 0.39 ms (well under 50% threshold), refresh happy + DB-row state (used_at + revoked_at on old; prev_token_id on new), refresh reuse 401 E1005 + family-revoked, refresh expired 401 (no family revoke), logout 200 + cookie clear + family-revoke + idempotent, **concurrent rotation race**: `Promise.all([refresh, refresh])` with same cookie → exactly `[200, 401]` (FOR UPDATE serialises; loser sees `used_at IS NOT NULL` → reuse path → fail-closed). Rate-limits also verified separately (LOGIN_IP_RATE_LIMIT=5 → 6th=429; LOGIN_EMAIL_RATE_LIMIT=10 → 11th=429). **New env vars surfaced** (`.env.example` + Dokploy env update due Plan 02-12 runbook): `JWT_SECRET` (REQUIRED, ≥32 B, fail-fast), `ACCESS_TOKEN_TTL` (default 900 s), `REFRESH_TOKEN_TTL` (default 2_592_000 s = 30d), `SERVER_IP_HASH_SECRET` (warn-fallback to unkeyed SHA-256 in dev), `LOGIN_IP_RATE_LIMIT` / `LOGIN_EMAIL_RATE_LIMIT` / `REFRESH_IP_RATE_LIMIT` (env-tunable for E2E). **Pino redaction extended**: `req.headers['cookie']`/`set-cookie`, `res.headers['Set-Cookie']`, `req.body.{accessToken,access_token,refreshToken,refresh_token}`, `res.body.{accessToken,wrappedMasterDek,wrappedMasterDekRecovery,wrappedUserSigningSk,wrappedUserKxSk}`. **Audit events emitted**: `auth.login.ok`, `auth.login.fail`, `auth.refresh.ok`, `auth.refresh.fail`, `auth.refresh.reuse_detected`, `auth.logout` — Phase 10 hash-chain consumes these. Five small deviations under rules 1-2: T1 wires the whole module (refresh+logout controllers can't be added incrementally without a missing-provider boot failure); /auth/params is GET (plan unspecified); DUMMY_HASH derived via SHA-256 not HKDF (functionally equivalent); SERVER_IP_HASH_SECRET warn-fallback mirrors 02-07's salt pattern; refresh-cookie value is base64url. No rule-4 deviations, no CHECKPOINT. **Plan 02-09 hand-offs**: replace 5 `FixedWindowRateLimiter` instances (login.ip, login.email, refresh.ip, signup, invite.redeem) with `@nestjs/throttler` + Redis at the same ceilings; formalise audit-event shape into a typed enum + helper. **Plan 02-11 hand-offs**: GET /auth/params first → Argon2id locally → POST /auth/login → 200 carries serverArgonSalt + wrapped material; access token in memory only (never localStorage); browser auto-sets the __Host-refresh cookie; auto-refresh ~60 s before exp; on E1005 hard-redirect to /login. See `.planning/phases/02-auth-crypto/02-08-SUMMARY.md`.

- **2026-04-29** — Phase 02 / Plan 07 (POST /invite/redeem + atomic POST /auth/signup) executed. Two atomic commits: `4ccfe83` (T1) `apps/api/src/{crypto,invite}/*` + Pino redaction extension + ErrorCodes (+AUTH_INVITE_EXPIRED/ALREADY_REDEEMED/SIGNUP_DUPLICATE_EMAIL); `fee780b` (T2) `apps/api/src/auth/signup/*` + `AllExceptionsFilter` honoring embedded `{error:{code,message}}`. New `CryptoService` (@Global) wraps `SERVER_INVITE_SECRET` / `SERVER_RECOVERY_HMAC_SECRET` HMAC peppers + `SERVER_ARGON_SALT` (16B; warn-fallback to random in-memory in dev) + env-tuned `argon2Params` with floor enforcement. `POST /invite/redeem` is informational ({code} -> {inviteId,email,argon2Params,serverArgonSalt(b64)}) — does NOT consume the invite; all failures collapse to single E1006 anti-enumeration; rate-limited 30/IP/h. `POST /auth/signup` is atomic: single Drizzle tx with `SELECT ... FOR UPDATE` -> INSERT users -> single-shot `UPDATE invite_codes WHERE redeemed_at IS NULL`; race-loser sees 0 rows + aborts; dup-email unique-violation (pg 23505 via `DrizzleQueryError.cause.code`) collapses to E1006 + tx rollback verified leaves invite unredeemed; KDF-downgrade defence (server checks `argon2Params >= floor`); rate-limited 3/IP/h via env-tunable `SIGNUP_RATE_LIMIT`. Zod `.strict()` provably rejects `password`/`secretKey`/`recoveryPhrase` etc. before any handler runs; every bytea field length-checked at API boundary (32B verifier, 32B userPubKey, 16B userArgonSalt, 32..256B wrap blobs). Pino redaction extended to 10 new signup-body paths. Audit-event `{evt:"signup",user_id,email,ts}` emitted (Phase 10 future-hash-chains). In-memory `FixedWindowRateLimiter` (placeholder; 02-09 swaps for `@nestjs/throttler` + Redis). E2E verified end-to-end against fresh `postgres:18.3-alpine` covering 10 scenarios: redeem-happy, redeem-invalid, redeem-expired, redeem-doesn't-consume, signup-happy, re-redeem-after-success, strict-reject-password-field, race (concurrent signups same inviteId -> exactly one 201 + one 400/E1006), rollback (dup email mid-tx -> invite stays unredeemed), rate-limit reachable. Signup catch-handler hardened to NOT log `err` directly (Drizzle errors include SQL params i.e. bytea ciphertexts) — logs `err_type` + `pg_code` only. **Auto-login decision: NO** (deferred to 02-08 landing a clean SessionService). **Plan 02-08 (login/refresh/logout) hand-offs documented** in `02-07-SUMMARY.md`: read `users.server_argon_salt` + `users.argon2_params` from row not env, share `CryptoService.constantTimeEqual`, follow same in-memory rate-limit pattern. **Plan 02-09 hand-offs**: replace `apps/api/src/common/rate-limit.ts` with `@nestjs/throttler` (Redis backend) for the two consumers (`invite.controller.ts` + `signup.controller.ts`); audit-events module formalises the structured Pino shape currently emitted. **Plan 02-10 web signup hand-off**: byte-exact request/response contract documented in SUMMARY; .strict() means web client must NOT send `email` or `server_argon_salt` in the signup body — server takes both from the locked invite row + env. New env vars consumed: `SERVER_INVITE_SECRET` (already in .env.example), `SERVER_RECOVERY_HMAC_SECRET` (already in .env.example), `SERVER_ARGON_SALT` (NEW — must add to .env.example before prod cutover; 16B base64), `ARGON2_MEMORY_KIB`/`ITERATIONS`/`PARALLELISM` (already), `SIGNUP_RATE_LIMIT`/`SIGNUP_RATE_WINDOW_MS` (NEW optional E2E escape hatches). Five small deviations under rules 1-2 documented (rate-limit choice; redeem body shape per plan over caller carry-over §2; signup body shape per plan over caller §1; AllExceptionsFilter additive change; internal-error sanitisation). No rule-4 deviations, no CHECKPOINT. See `.planning/phases/02-auth-crypto/02-07-SUMMARY.md`.

- **2026-04-29** — Phase 02 / Plan 06 (operator CLI: `pnpm cli invite create` + `pnpm cli argon2 calibrate`) executed. Two atomic commits: `93f8510` (T1) apps/cli scaffold + invite create; `47c970c` (T2) argon2 calibrate. New workspace package `@simplevault/cli` (apps/cli) using commander v12.1.0, `type=module`, NodeNext. `invite create --email <addr> [--ttl-days N | --expires-in 14d]`: 16 random bytes → Crockford base32 (no I/L/O/U) → hyphenated every 4 chars (`XXXX-XXXX-…-XX`); `code_hash = HMAC-SHA256(SERVER_INVITE_SECRET, raw_code_bytes)`; INSERT into `invite_codes` with email lowercased + `expires_at = now + ttl` (default 7 days, max 365); raw code printed once to stdout, no pino, no file logging. `argon2 calibrate`: invokes `@simplevault/crypto/node` `calibrate(750, 250)`, prints measured ms + discovered Argon2id params + a Dokploy-paste-ready snippet (`ARGON2_MEMORY_KIB` / `ARGON2_ITERATIONS` / `ARGON2_PARALLELISM`); warns if at-or-below floor (m=19456,t=2,p=1) or hit memory cap (256 MiB). Root `package.json` scripts added: `cli` (`pnpm --filter @simplevault/cli exec node dist/main.js`) + `cli:build`. Verified e2e against `postgres:18.3-alpine` + drizzle migrations: two distinct invocations produced two distinct codes, both rows landed with non-null `code_hash` + lowercased email + correct `expires_at`; raw code never written to DB. Fail-fast verified (exit 2 on missing `SERVER_INVITE_SECRET` / `DATABASE_URL`). Three load-bearing landings carry beyond apps/cli: (a) **`@simplevault/crypto/node` subpath export added** to packages/crypto/package.json so server-side consumers get types + Node barrel in lockstep (strictly additive, non-breaking); (b) **libsodium-wrappers-sumo@0.7.16 patched** via `pnpm.patchedDependencies` (`patches/libsodium-wrappers-sumo@0.7.16.patch`) to point its `import` condition at the shipped CJS bundle — fixes upstream `.mjs` resolution bug under raw Node, same root cause as the existing vitest alias in packages/crypto/vitest.config.ts; (c) **.env.example documents** `SERVER_INVITE_SECRET` (CLI + signup redeem), `SERVER_RECOVERY_HMAC_SECRET` (deferred from 02-05; first consumed by 02-07), `ARGON2_MEMORY_KIB`/`ITERATIONS`/`PARALLELISM` (operator-tunable). New `docs/operator/CLI.md` stub (Phase 12 will round it out). Drizzle dev-mode query logger suppressed at the CLI process boundary by forcing `NODE_ENV=production` if unset/dev — would otherwise leak the HMAC `code_hash` to stdout. Deviations (auto rules 1-2): added `apps/cli/src/lib/base32.ts` for testability; added crypto `./node` subpath; libsodium pnpm patch; env var names emitted are `ARGON2_MEMORY_KIB`/`ITERATIONS`/`PARALLELISM` (matching `Argon2Params` field names from 02-02) rather than the truth's vendor-y `ARGON2_MEMORY_COST`/`ARGON2_TIME_COST` wording. No rule-4 deviations, no CHECKPOINT. **Plan 02-07 (signup) UNBLOCKED** — redeem-path contract documented in 02-06-SUMMARY §"Carry-overs": HMAC over the *string bytes* of the submitted code (CLI hashed `Buffer.from(code, "utf8")`, not raw entropy bytes); atomic single-shot UPDATE pattern (`WHERE id=$1 AND redeemed_at IS NULL`); email binding via lowercased `email`. **Plan 02-08 (login/refresh/logout) UNBLOCKED** in parallel with 07. See `.planning/phases/02-auth-crypto/02-06-SUMMARY.md`.

- **2026-04-28** — Phase 02 / Plan 04 (X25519 sealed-box + cross-runtime symbol-parity test + `node.ts` server-side tighten) executed in TDD mode. Four atomic commits: `91b7410` RED (sealed-box test stub), `6acaa56` GREEN (`src/sealed-box.ts` + barrel wiring; `generateKxKeyPair` / `sealedBoxSeal` / `sealedBoxOpen` / `KxKeyPair` / `SealedBoxKeyLengthError`), `381103e` REFACTOR (`src/node.ts` switched from glob `export *` to explicit named re-exports — server barrel now strictly excludes every function taking `master_password` / `secret_key` / `mnemonic`: drops `deriveMasterKek`, `deriveRecoveryKek`, `generateMnemonic`/`generateMnemonicAsync`, `validateMnemonic`, `mnemonicToSeed`, `computeRecoveryLookupHash`; per-suite parity tests in `key-hierarchy.test.ts` + `bip39.test.ts` updated to assert the deliberate divergence), `b10832d` (Task 2: `test/parity.test.ts` snapshot of shared + browser-only + node-only sets, with kind-equality and value-equality checks for the deep-frozen constants; `src/index.ts` adds types-only re-exports for `@simplevault/crypto/types`; two source-comment rewords to keep the canonical `dist/browser.js` purity grep `(node:|require\(.crypto.)` clean even when scanning comments). 69 tests passing across 7 suites (62 → 69; +9 sealed-box, +7 parity). Truths verified: X25519 keypair = 32B pub + 32B sk; sealed-box round-trip OK; tampered-ciphertext throws; wrong-sk throws; 32-byte vault_DEK end-to-end seal+open bytes-equal; symbol-parity test snapshot matches; `index.ts` types surface compiles. Browser bundle purity: `grep -E '(node:|require\(.crypto.)' packages/crypto/dist/browser.js` returns ZERO; same for every transitively-included file under `packages/crypto/dist/`. Bundle sizes: `dist/browser.js` 1.3 KB, `dist/node.js` 3.1 KB — divergence is deliberate (Node uses explicit named re-exports vs Browser's `export *` for type-level invariant enforcement); the underlying module graphs are equivalent and the plan's "within 5%" guideline is auto-deviated under rule 1 (cosmetic). No rule-4 deviations, no CHECKPOINT. Standard `crypto_box_*` functions are present at runtime in `libsodium-wrappers-sumo` but missing from `@types/libsodium-wrappers-sumo@0.7.8`; we declare a typed `SodiumBoxSurface` view and `as unknown as` cast through it once, keeping all call sites strict. Naming convention: libsodium's `privateKey` is exposed as `secretKey` everywhere for consistency with the SimpleVault DB column `users.wrapped_user_kx_sk` and libsodium CLI/docs. **Plan 02-06 (operator CLI) UNBLOCKED.** Plan 02-07 (signup) hand-off documented in 02-04-SUMMARY: at signup the browser generates X25519 (`generateKxKeyPair`) and Ed25519 (deferred — 02-07 must add `crypto_sign_keypair` wrapper, possibly as `sign.ts` sibling) keypairs, sends `users.user_pub_key` in the clear, wraps `signingSk` under `master_KEK` with AAD contextId `"sv:user-sign-sk:v1|" + user_id_bytes` → `users.wrapped_user_signing_sk`, wraps `kxSk` similarly with `"sv:user-kx-sk:v1|" + user_id_bytes` → `users.wrapped_user_kx_sk`. Phase 07 (shared vaults) is now unblocked crypto-side. See `.planning/phases/02-auth-crypto/02-04-SUMMARY.md`.

- **2026-04-28** — Phase 02 / Plan 03 (BIP-39 + key hierarchy + recovery wrap) executed in TDD mode. Four atomic commits (RED+GREEN per task; refactor folded into GREEN per established 02-02 pattern). New modules: `src/bip39.ts` exports `generateMnemonic` (libsodium-randombytes-backed entropy, NOT bip39's default crypto.randomBytes — preserves browser parity), `generateMnemonicAsync`, `validateMnemonic`, `mnemonicToSeed` (returns plain Uint8Array, not Buffer), `computeRecoveryLookupHash` (NFKD + lowercase + single-spaced canonicalisation → sha256, server then applies outer HMAC at API layer); `src/key-hierarchy.ts` exports `deriveMasterKek({password, secretKey, email, userArgonSalt, argon2Params?})` (HKDF-SHA256 over secret_key with salt=lower(email) and info=`simplevault.v1.master_seed` → master_seed; XOR with 16-byte user_argon_salt → argon_salt; Argon2id → 32-byte master_KEK), `deriveRecoveryKek({mnemonic, userId})` (HKDF-SHA256 over BIP-39 seed with salt=userId and info=`simplevault.v1.recovery_kek` → 32-byte recovery_KEK; no Argon2 — BIP-39 seed already 256-bit), `wrapKey`/`unwrapKey` (thin AEAD wrappers binding the AAD downgrade defence), `encodeAad(params, contextId)` (canonical 13-byte prefix `[version=0x01][m_be32][t_be32][p_be32]` + contextId — LOAD-BEARING wire format frozen for v1; Plan 07 server signup MUST replicate byte-for-byte), `AAD_VERSION=0x01`. All re-exported through both `src/browser.ts` and `src/node.ts` barrels — symbol parity asserted at runtime by per-suite barrel-parity tests. 53 tests passing across 5 suites (`bip39.test.ts` 13, `key-hierarchy.test.ts` 17, plus prior 23 from Plan 02). **Browser bundle parity verified:** `grep -nE '^\s*(import\|export)\s.*[\"](node:\|crypto\|buffer)[\"]' dist/*.js` returns ZERO. Build clean. **Round-trip + AAD-downgrade truths verified:** master-path round-trip OK; recovery-path round-trip OK; dual-path test asserts SAME master_DEK reachable via either KEK; mutating params bytes, contextId, OR version byte in AAD causes `unwrapKey` to throw. **Server-side invariant preserved:** crypto package's `node.ts` exports the same symbols, but `computeRecoveryLookupHash`/`deriveMasterKek`/`deriveRecoveryKek` are browser-only by convention — Plan 07 must keep its server route from invoking them (no type-level enforcement). Two minor deviations auto-applied under rules 1-2: (a) initial bip39 RED test mutated word[0] to a different valid wordlist word which can occasionally validate against a different entropy — replaced with two stronger mutation tests (non-wordlist typo + loop-up-to-16 last-word swap); (b) refactor commits folded into GREEN. No rule-4 deviations, no checkpoints. **HKDF info-strings frozen for v1:** `"simplevault.v1.master_seed"` and `"simplevault.v1.recovery_kek"`. **Plan 02-04 (X25519 sealed-box) UNBLOCKED** — it generates the user keypair at signup and consumes `wrapKey`/`encodeAad` to wrap the X25519 + Ed25519 secret keys under master_KEK. **Plan 02-07 (signup API) UNBLOCKED** — its column-fill contract is documented in §"Plan 07 consumption checklist" in `.planning/phases/02-auth-crypto/02-03-SUMMARY.md`. See SUMMARY for the full AAD wire format spec, public API surface, and consumption checklist.

- **2026-04-29** — Phase 02 / Plan 02 (packages/crypto Argon2id + AEAD + calibrate) executed in TDD mode. Six atomic commits (3 RED + 3 GREEN; refactor folded into GREEN since barrel re-exports were the only candidate and parity tests already cover them inline). New modules: `src/argon2id.ts` exports `deriveKey`, constant-time `verify`, memoised `ready`, `paramsToOpsMem`, frozen `ARGON2_DEFAULT_PARAMS` (m=64MiB,t=3,p=1) + `ARGON2_FLOOR_PARAMS` (m=19MiB,t=2,p=1) per CRYPTO-STACK.md, `Argon2SaltTooShortError` for sub-16-byte salts; `src/aead.ts` exports XChaCha20-Poly1305-IETF `encrypt`/`decrypt` with **mandatory** AAD parameter (`Uint8Array` only — callers pass `new Uint8Array(0)` for "no AAD"; defends against silent omission), 24-byte random nonce per call, `AeadKeyLengthError` for non-32-byte keys; `src/calibrate.ts` exports binary-search `calibrate(targetMs=750, toleranceMs=250)` returning `{params, measuredMs}`, bounded by floor + new `ARGON2_MEMORY_CAP_KIB` (256 MiB) ceiling, 1-D tuning surface (only `memoryKiB` moves; iterations + parallelism stay at defaults). All three modules re-exported through both `src/browser.ts` and `src/node.ts` barrels — symbol parity asserted at runtime by vitest tests in each suite. 23 tests passing across 3 suites (`argon2id.test.ts` 9, `aead.test.ts` 9 — incl. 10 000-encrypt nonce-uniqueness smoke, `calibrate.test.ts` 5). **Browser bundle parity verified:** `grep -nE 'require\(.crypto.|require\(.buffer.|from .node:|from "crypto"|node:crypto|node:buffer' packages/crypto/dist/browser.js` returns ZERO matches; `dist/browser.js` and `dist/node.js` both `export * from "./argon2id.js"`, `"./aead.js"`, `"./calibrate.js"`. Vitest 2.1.9 added as devDep to packages/crypto only; vitest config carries a `libsodium-wrappers-sumo` CJS alias to work around an upstream ESM-packaging bug (the published `.mjs` references a sibling `libsodium-sumo.mjs` not present in the package). Three minor deviations auto-applied under rules 1-3: (a) sanity-latency floor lowered `>100ms`→`>50ms` on Apple Silicon dev hardware (Argon2id m=64MiB,t=3 lands ~93ms there — operator should expect 500-800ms range on prod VPS); (b) refactor commits folded into GREEN; (c) vitest libsodium CJS alias. No rule-4 deviations, no checkpoints. Truths verified: Argon2id round-trip + constant-time verify; default + floor params exported as code-defined constants; calibrate returns params within band OR caps; AEAD round-trip preserves plaintext on AAD match + throws on mutation; 24-byte nonces fresh per call (verified by 10 000-sample collision check); AAD must include argon2 params bytes (mandatory in API type, callers MUST encode + pass — this is a documented downstream-plan requirement, not a packages/crypto invariant); Vitest suite green. **Plan 02-03 (BIP-39 + key hierarchy) UNBLOCKED** — it consumes `deriveKey` for `deriveMasterKEK` + `encrypt`/`decrypt` for wrapping the key hierarchy. **Plan 02-06 (operator CLI) UNBLOCKED** for its `argon2 calibrate` subcommand. See `.planning/phases/02-auth-crypto/02-02-SUMMARY.md`.
- **2026-04-29** — Phase 02 / Plan 01 (NestJS 10 → 11 upgrade) executed. Two atomic commits: `f205f68` (T1) bumps `@nestjs/common`/`@nestjs/core`/`@nestjs/platform-express` to `^11.0.0` (resolved 11.1.19), `@nestjs/config` to `^4.0.0` (4.0.4), `@nestjs/cli` + `@nestjs/schematics` (dev) to `^11.0.0`, and `nestjs-pino` to `^4.6.0` (4.6.1 — declares Nest 11 peer, no need for v5/v6 line); `4b27606` (T2) removes the `multer@<2.1.1: >=2.1.1` entry from root `pnpm.overrides` (lockfile resolves `multer@2.1.1` naturally via Nest 11's platform-express). Lodash override retained (`@nestjs/config@4` still drags lodash). All 6 plan truths verified: build / typecheck / lint green, api boots, `curl /health` → canonical `{status:degraded,db:down,redis:down,…}` 200 in DB-less env. `pnpm audit --prod --audit-level=high`: 0; `pnpm audit --audit-level=high` (incl dev): 0 — the two Phase 01 dev-only Highs (`glob` CLI command-injection + `picomatch` ReDoS via `@nestjs/cli@10.4.9`) are GONE. Two moderate residuals remain, both dev-only transitives (esbuild via drizzle-kit > @esbuild-kit/* chain; postcss via next 15.5.15) — pre-existing, not high/critical, no production impact. Source compatibility with Nest 10→11 was clean — no edits needed to `apps/api/src/main.ts` or `app.module.ts`. No deviations. See `.planning/phases/02-auth-crypto/02-01-SUMMARY.md`. Wave 2 (Plans 02, 03, 05) is now unblocked.
- **2026-04-29** — Phase 02 (Auth + Crypto core) **PLANNED**. INDEX + 12 plans created under `.planning/phases/02-auth-crypto/`, structured into 7 waves to maximise parallelism (sequential 12× → parallel ~7×, ~42% saving). **Wave 1**: Plan 01 NestJS 10→11 upgrade (closes Phase 01 multer pnpm.override + dev-only @nestjs/cli@10.4.9 Highs). **Wave 2 (3-track parallel)**: Plan 02 (TDD: Argon2id + AEAD + calibrator), Plan 03 (TDD: BIP-39 + key hierarchy + recovery wrap), Plan 05 (DB schema: extend users + add user_sessions + invite_codes + Drizzle migration on PG 18.3). **Wave 3 (2-track parallel)**: Plan 04 (TDD: X25519 sealed-box + cross-runtime export-parity test), Plan 06 (operator CLI: `pnpm cli invite create --email …` + `pnpm cli argon2 calibrate`). **Wave 4 (2-track parallel)**: Plan 07 (POST /invite/redeem + atomic POST /auth/signup), Plan 08 (POST /auth/login with timing-floor + dummy-Argon2id-on-miss + JWT issuance + POST /auth/refresh with single-use rotation + family-revoke-on-reuse + POST /auth/logout). **Wave 5**: Plan 09 (GET /me with strict allow-list serialisation + JwtAuthGuard + Pino redaction extension + global throttler config + audit-events typed module for Phase 10 future-proofing). **Wave 6 (2-track parallel)**: Plan 10 (web /signup multi-step state machine with secret-key + recovery-phrase confirmation gates that cannot be bypassed in DOM, in-memory keyStore + wipe()), Plan 11 (web /login + login-derivations + access-token-in-memory store + auto-refresh hook + (authed) route group + /me page + logout). **Wave 7**: Plan 12 (Cypress happy + sad path E2E + CI e2e job + operator runbook updates for new env vars + Argon2id calibration). 3 TDD plans (02, 03, 04). Operator decisions surfaced in INDEX: (a) SMTP deferred to Phase 07 (out-of-band invite delivery for v1), (b) Argon2id calibration is a one-time operator action on the VPS via `pnpm cli argon2 calibrate` then set ARGON2_MEMORY_COST/ARGON2_TIME_COST in Dokploy env, (c) JWT_SECRET + SERVER_INVITE_SECRET + SERVER_RECOVERY_HMAC_SECRET + SERVER_ARGON_SALT + SERVER_EVENT_SECRET must each be 32B random + set in Dokploy before deploy. Phase-02 security gate auditors per ROADMAP: crypto-auditor + auth-flow-auditor + owasp-top10-auditor + input-validation-auditor + rate-limit-dos-auditor (5 blocking) + threat-modeler (informational, updates Login flow STRIDE + AT-5). 13 goal-backward truths anchored to ROADMAP "Done when" line. Hard-things-to-watch flagged in INDEX §key-links: Argon2id calibration drift, packages/crypto browser bundle purity (no node:crypto), server-stores-only-verifier invariant, recovery phrase never reaches server, /me allow-list, refresh rotation race + family-revoke, timing-floor-not-setTimeout, extended Pino redaction list, NestJS 11 upgrade timing, exports-map parity, audit-event future-proofing, JWT kid claim for future rotation. Ready for `/gsd:execute-phase 2`.
- **2026-04-29** — Phase 01 security gate **PASSED on re-run** and Phase 01 officially **CLOSED**. Three agents re-spawned: gsd-verifier flipped to status=passed, 11/11 truths (Truth 4 closed by FINDING-0007 fix; no regressions on Truths 5-7/9-11 from dep bumps); infra-deployment-auditor PASS (FINDING-0005/-0006 VERIFIED-CLOSED; postgres + redis healthy with `cap_drop:[ALL]` + minimal cap_add, `no-new-privileges:true`); dependency-supply-chain-auditor PASS (FINDING-0001..0004 VERIFIED-CLOSED; lockfile resolves next@15.5.15, drizzle-orm@0.45.2, drizzle-kit@0.31.10, multer@2.1.1; `pnpm audit --prod --audit-level=high` reports 0 high). Single new finding surfaced + closed in same cycle: FINDING-0009 lodash@4.17.21 (GHSA-r5fr-rjxr-66jc) — upstream patch (>=4.18.0) became available between 2026-04-28 and 2026-04-29; closed by adding `"lodash@<4.18.0": ">=4.18.1"` to root `pnpm.overrides` (commit `ac55411`), lockfile resolves lodash@4.18.1 single-resolution. Tracked tech debt: two `pnpm.overrides` (multer + lodash) to remove with NestJS 11 upgrade in Phase 02; dev-only Highs (glob CLI command-injection + picomatch ReDoS via `@nestjs/cli@10.4.9`) accepted for Phase 01, will clear with Phase 02. New audit-log entries (4) and audit reports (3) committed. **Phase 01 — Foundations: CLOSED.**
- **2026-04-28** — Phase 01 gap-closure executed (response to `/gsd:verify-work 1` initial FAIL). All 8 findings (FINDING-0001..0008) moved to FIXED-PENDING-VERIFICATION across 6 atomic commits: `59c2e19` next 15.1.0 → ^15.5.15 + typedRoutes promoted out of experimental (closes -0001/-0002, CSP nonce middleware re-verified via curl); `8a31481` drizzle-orm 0.38 → ^0.45.2 + drizzle-kit 0.30 → ^0.31.10 (closes -0003, db:generate produced no schema diff so existing migration unchanged, end-to-end verified against postgres 18.3-alpine — apply + idempotent re-run); `71c6399` pnpm.overrides multer@<2.1.1: ">=2.1.1" (closes -0004 — multer resolves to 2.1.1 in all paths, audit clean for multer; full Nest 10→11 upgrade deferred to Phase 02 to avoid major-bump churn now); `579ea8d` cap_drop: [ALL] on postgres + redis (closes -0005/-0006, postgres needs SETUID/SETGID/DAC_READ_SEARCH/CHOWN/FOWNER added back for initdb gosu, redis needs SETUID/SETGID for entrypoint user-drop — both verified healthy); `31574f8` add `dev` script to apps/api (closes -0007, Truth 4 literal-wording gap); `bac1fa3` weekly cron `0 5 * * 1` on container-scan.yml (closes -0008). Residual high-severity prod audit finding `lodash@4.17.21` via `@nestjs/config` is the previously-documented unfixable upstream advisory (no patched lodash version exists yet); auditor flagged as accepted residual. Awaiting `/gsd:verify-work 1` re-run.
- **2026-04-28** — Phase 01 / Plan 10 (README + operator runbooks) executed. Four docs added: `README.md` (30-second pitch + CI/Container-Scan badges + links to operator docs and `.planning/`), `docs/operator/LOCAL-DEV.md` (fresh-clone → `curl /health` ok in <5min, daily-dev hot-reload workflow, migrations, troubleshooting, nuke-and-pave), `docs/operator/DOKPLOY-DEPLOY.md` (Dokploy app config for `pass.runadev.com` — build context = repo root + Dockerfile path = `apps/<app>/Dockerfile` called out as the easy-to-make mistake, internal-port + healthcheck + domain, Traefik handles TLS no host port mapping, env-var mapping table for api + web, networking, deploy + verify + rollback caveat), `docs/operator/SECURITY-NOTES.md` (operator-must list: secrets to back up offline, off-site rsync target prep with `command="rrsync -wo …"` blast-radius restriction, audit-checkpoint repo decision, SMTP provider choice, operator 2FA self-enrollment, periodic tasks, detection signals). Two atomic commits (T1: README+LOCAL-DEV, T2: DOKPLOY-DEPLOY+SECURITY-NOTES). No deviations. Five operator-decision items surfaced in SUMMARY for downstream phases: SMTP provider (Phase 02), audit checkpoint repo location (Phase 10), off-site backup target + SSH keypair (Phase 14), operator-account 2FA policy (Phase 14), `@germankatz` CODEOWNERS confirmation. **Phase 01 (Foundations) COMPLETE — all 10 plans across 7 waves done. Awaiting `/gsd:verify-work 1` security gate.** See `.planning/phases/01-foundations/01-10-SUMMARY.md`.
- **2026-04-28** — Phase 01 / Plan 08 (drizzle migration generation + PG 18.3 verify) executed. First migration `packages/db/drizzle/0000_talented_microchip.sql` generated from the `users` stub schema (221 bytes, sha256 `e58c42df…bc7793`); journal + snapshot populated. PG 18.3 + Drizzle stack verified end-to-end across three vectors and certified in `.planning/phases/01-foundations/01-08-COMPAT.md`: (1) direct `node ./dist/migrate.js` from a `node:22-alpine` container joined to `simplevault_backend` applies the migration cleanly to PG 18.3 (creates `drizzle.__drizzle_migrations` + `public.users` in one tx); (2) re-running is an idempotent no-op (journal lookup, no DDL); (3) `docker compose up -d --build api` against a wiped DB shows the api prestart hook running the migration via `migrate-then-start.sh` then `Nest application successfully started`. Verified versions (load-bearing for the deployment auditor gate): drizzle-orm 0.38.4, drizzle-kit 0.30.6, pg 8.20.0, postgres 18.3-alpine, node 22-alpine. Root `db:generate`/`db:migrate`/`db:studio` scripts added to `package.json`. Deviation: `drizzle.config.ts` `schema` field changed from `./src/schema/index.ts` to `["./src/schema/users.ts"]` because Drizzle Kit's CJS loader can't follow the barrel's NodeNext `./users.js` import — future tables must be appended explicitly to this list (a glob with exclusions is a possible follow-up). Plan's CHECKPOINT decision was NOT triggered — no PG 18 incompat surfaced. Network-join approach used (option B); host-port override + `docker-compose.override.yml` were not needed and not created. See `.planning/phases/01-foundations/01-08-SUMMARY.md` and `01-08-COMPAT.md`. Wave 7 Plan 10 (docs) still pending.
- **2026-04-28** — Phase 01 / Plan 07 (local docker-compose + `.env.example`) executed. `docker-compose.yml` brings up postgres-18.3-alpine + redis-7.4-alpine + api + web. Hardening verified: postgres + redis have NO host port mapping (`docker compose port` returns `:0`); `backend` network is `internal: true` (egress-blocked); api on both `backend` + `frontend`, web only on `frontend`; api/web `read_only: true` rootfs with `tmpfs: /tmp` (64m, mode 1777) for pino; `cap_drop: [ALL]` + `security_opt: [no-new-privileges:true]` + `mem_limit` (256–512m) + `pids_limit` (100–200) per service; api/web ports bound to `127.0.0.1` only. Healthchecks on all 4 (`pg_isready`, `redis-cli ping`, Dockerfile HEALTHCHECK on api/web); `depends_on: condition: service_healthy` so api waits for pg+redis. Verified `curl http://localhost:3001/health → {status:ok,db:ok,redis:ok}` and `curl -sI http://localhost:3000/ → 200 + full CSP`. Stack-up wall-time ~22s after Plan 06 cached images. `.env.example` documents every var consumed by compose + Dockerfiles (DATABASE_URL, REDIS_URL, JWT_SECRET, SERVER_CHAIN_SECRET, NODE_ENV from turbo.json passThroughEnv, plus POSTGRES_*, CORS_ORIGINS, NEXT_PUBLIC_API_URL, LOG_LEVEL, PORT) with safe dev-only placeholders + Dokploy prod-mapping notes. Deviations: hardening tightened beyond plan (api `read_only: true` immediately rather than deferred to Phase 12; web also `read_only: true`); redis pinned to 7.4-alpine. `users` table not present yet — expected, Plan 08 generates the migration. See `.planning/phases/01-foundations/01-07-SUMMARY.md`. Wave 6 complete; Wave 7 Plans 08 + 10 unblocked (parallel).
- **2026-04-28** — Phase 01 / Plan 09 (GitHub Actions CI pipeline) executed. Four files added under `.github/`: `workflows/ci.yml` (lint + typecheck + build + `pnpm audit --audit-level=high` on every PR/push to main, plus `workflow_dispatch`; Node 22 + pnpm 9.15.0 via dedicated `pnpm/action-setup` (NOT setup-node's packageManager arm); StepSecurity `harden-runner` in audit egress mode; `actions/cache` keyed on `pnpm-lock.yaml`; concurrency cancel-in-progress; `permissions: contents: read`; 15-min timeout, plan target <8 min once cache warm), `workflows/container-scan.yml` (Trivy CRITICAL+HIGH matrix scan of api+web images on PRs touching Dockerfiles / `pnpm-lock.yaml` / package.json or main push of Dockerfiles; build context = repo root; cache via type=gha; `fail-fast:false`, `ignore-unfixed:true`, `exit-code:1`), `dependabot.yml` (weekly Monday npm + github-actions + per-app docker; grouped: nest, next, drizzle, typescript, eslint), `CODEOWNERS` (`@germankatz` owns everything; security-critical paths called out for future team-mode enforcement). All third-party actions pinned by full commit SHA + version comment (supply-chain hygiene). YAML validated locally (`actionlint` unavailable, fell back to ruby YAML parse). Workflows not yet exercised on GitHub — first PR/push will validate runtime + cache behavior + audit findings. No deviations from plan. See `.planning/phases/01-foundations/01-09-SUMMARY.md`. Wave 5 complete; Wave 6 Plan 07 (docker-compose) unblocked.
- **2026-04-28** — Phase 01 / Plan 06 (production Dockerfiles for apps/api + apps/web) executed. Both Dockerfiles are multi-stage (deps -> build -> runner) on `node:22-alpine` with pnpm 9.15.0 via corepack, tini PID 1, non-root `app` user (uid 100), HEALTHCHECK wired, EXPOSE only the app port. apps/api: `pnpm --filter @simplevault/api deploy --prod /out` produces a self-contained bundle; ENTRYPOINT runs `migrate-then-start.sh` which exec's a new tiny migrate runner (`packages/db/src/migrate.ts` -> `dist/migrate.js`) before `node dist/main.js` — fail-fast on migration error (`set -eu`), idempotent (drizzle's `__drizzle_migrations` table). apps/web: Next 15 standalone output; added `outputFileTracingRoot: <repo-root>` to `next.config.mjs` so workspace deps (`@simplevault/shared`) are bundled. Verified non-root, healthcheck, ECONNREFUSED-on-bad-DB, full CSP/HSTS/COOP/CORP/Permissions-Policy headers all present on web 200. Image sizes: api 329MB (target <300, slightly over), web 328MB (target <200, over — Next 15 standalone is heavier than v14; revisit with distroless in Phase 12/13). Deviations: dropped pnpm-10-only `--legacy` flag from `pnpm deploy`; added empty `packages/db/drizzle/meta/_journal.json` placeholder (Plan 08 will populate); migrations folder lives at `/app/node_modules/@simplevault/db/drizzle` (post-pnpm-deploy layout), not `/app/packages/db/drizzle`. `.dockerignore` (root + per-app) keeps build contexts <5MB. Dokploy build config documented (build context = repo root, Dockerfile path = `apps/<app>/Dockerfile`). See `.planning/phases/01-foundations/01-06-SUMMARY.md`. Wave 5 Plan 09 (CI) still pending; Wave 6 Plan 07 (docker-compose) unblocked once 09 lands or in parallel.
- **2026-04-28** — Phase 01 / Plan 05 (apps/web Next.js 15 App Router + security-headers middleware) executed. Next.js 15.1.0 + React 19.0.0 with `output: "standalone"` for Phase 06 Dockerfile, Tailwind v3.4.17 (class dark mode, dark-by-default zinc-950), placeholder home page (`lang="es"`, robots noindex/nofollow). Strict per-request CSP middleware: 16-byte base64 nonce on every request (matcher excludes `_next/static`, `_next/image`, `favicon.ico`, `robots.txt`, `sitemap.xml`), `script-src 'self' 'nonce-...' 'strict-dynamic'`, no `unsafe-inline`/`unsafe-eval`, plus HSTS preload, X-Frame-Options DENY, Referrer-Policy no-referrer, Permissions-Policy (geolocation/mic/camera/payment/usb/FLoC blocked), COOP/CORP same-origin, X-Content-Type-Options nosniff, X-DNS-Prefetch-Control off. Nonce propagated to server components via `x-nonce` request header. Verified via `curl -sI`: all headers present on 200 + 404, two consecutive requests produce different nonces. Decisions: Tailwind v3 over v4-alpha (stability), `eslint-config-next` integration deferred (still legacy non-flat-config; non-blocking warning during `next build`). Notable deviations: middleware uses bundler-style imports (no `.js` extension) since Next webpack ignores NodeNext rules; Uint8Array `.reduce()` from plan replaced with `for...of` for TS strictness. See `.planning/phases/01-foundations/01-05-SUMMARY.md`. Wave 4 complete; Wave 5 (Plans 06 Dockerfiles + 09 CI, parallel) is now unblocked.
- **2026-04-28** — Project initialized via `/gsd:newproject`. PROJECT.md, REQUIREMENTS.md (with v1 + v2 + permanent-out-of-scope sections), ROADMAP.md (14 phases / 8 milestones, every phase with explicit security-auditor gate), `.planning/security/` scaffold (THREAT-MODEL, AUDIT-LOG, FINDINGS, AGENTS), and STATE.md created. Research outputs: CRYPTO-STACK.md, FEATURES.md, ARCHITECTURE.md, PITFALLS.md.
- **2026-04-28** — Deployment target confirmed: Dokploy at `pass.runadev.com`, managed Postgres 18.3, fresh Redis, app-level pg_dump backups (replaces restic sidecar), Traefik (Dokploy) replaces Caddy, security headers move to app layer. REQ-INFRA-001/002/005, REQ-WEBSEC-001..004, and Phase 01/14 of ROADMAP.md to be reflected in their next edit (deferred until /gsd:plan-phase 1 — adjustments will be done in-phase, not in upfront docs).
- **2026-04-28** — Phase 01 / Plan 04 (apps/api NestJS skeleton + `/health`) executed. NestJS 10 service on :3001 with helmet (strict CSP + HSTS), restrictive CORS allowlist, global ValidationPipe (whitelist+forbidNonWhitelisted+transform), AllExceptionsFilter returning canonical `{error:{code,message,requestId}}`, nestjs-pino with sensitive-field redaction (password, secretKey, recoveryPhrase, jwt, etc.), DbService (real `SELECT 1` ping via @simplevault/db pool) + RedisService (real `PING` via ioredis) wired as @Global modules, HealthService aggregating both into HealthResponse shape from @simplevault/shared/zod. Notable deviations: app declared `"type":"module"` and tsconfig overrides to NodeNext (required to consume ESM workspace packages); class-validator + class-transformer + pg + @types/pg added (runtime peers / direct type imports); DbService no longer throws on missing DATABASE_URL — degrades to `db:down` so /health stays 200 with status:degraded as plan's verify expected; tsBuildInfoFile pinned per-package to avoid root collision. `migrate-then-start.sh` deferred to Plan 06 alongside the Dockerfile. See `.planning/phases/01-foundations/01-04-SUMMARY.md`. Wave 4 Plan 05 (apps/web) still pending; Wave 5 (Dockerfiles + CI) blocked on Plan 05.
- **2026-04-28** — Phase 01 / Plan 03 (shared/crypto/db skeletons) executed. `packages/shared` ships error code map (E1xxx-E5xxx) + `HealthResponseSchema` Zod placeholder + barrel; `packages/crypto` ships branded types + `CryptoApi` interface + `browser.ts`/`node.ts` Phase-02 stubs with the load-bearing conditional exports map (`browser` before `node` before `default`); `packages/db` ships Drizzle `createDbClient` factory + `users` schema stub + `drizzle.config.ts` (`postgresql` dialect). All build / typecheck / lint clean. Resolved deps: zod 3.25.76, libsodium-wrappers-sumo 0.7.16, @noble/hashes 1.8.0, bip39 3.1.0, drizzle-orm 0.38.4, drizzle-kit 0.30.6, pg 8.20.0. See `.planning/phases/01-foundations/01-03-SUMMARY.md`. Wave 4 (Plans 04 apps/api + 05 apps/web, parallel) is now unblocked.
- **2026-04-28** — Phase 01 / Plan 02 (shared tsconfig + eslint-config packages) executed. `packages/tsconfig` exposes base/library/nestjs/nextjs presets; `packages/eslint-config` exposes ESLint v9 flat-config base + `/nest` + `/next` (typescript-eslint v8.59 strict + stylistic, import/order, no-enums, no-floating-promises). `eslint-config-next` deliberately deferred. See `.planning/phases/01-foundations/01-02-SUMMARY.md`. Wave 3 (Plan 03 — shared/crypto/db skeletons) is now unblocked.
- **2026-04-28** — Phase 01 / Plan 01 (root scaffold) executed. Turborepo + pnpm workspace bootstrapped: package.json, pnpm-workspace.yaml, turbo.json, tsconfig.base.json, .npmrc (strict + isolated), .nvmrc (22), .gitignore, .editorconfig, apps/ + packages/ stubs, pnpm-lock.yaml committed. `pnpm install` clean, `turbo run build` returns "no tasks". See `.planning/phases/01-foundations/01-01-SUMMARY.md`. Wave 2 (Plan 02 — tsconfig + eslint packages) is now unblocked.
